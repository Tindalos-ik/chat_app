# 聊天信息收发

本文按当前仓库的真实代码整理桌面客户端、`ChatServer1/2`、Redis、MySQL 和 gRPC
之间的文本聊天收发流程。重点是“现在代码实际做了什么”，并把已经预留但尚未完成的
会话、历史消息和离线消息能力单独标出来。

> 一句话概括：客户端登录后维持一条到 ChatServer 的 TCP 长连接；发送文本时先按
> JSON 协议发给本机 ChatServer，服务端验证发送者身份，再根据 Redis 中的在线路由
> 选择本机 TCP 推送或跨 ChatServer 的 gRPC 转发。当前只保证在线转发，不保存聊天记录。

## 1. 涉及的组件与职责

| 组件 | 主要职责 | 聊天消息中的位置 |
| --- | --- | --- |
| Qt 桌面客户端 | 编辑消息、渲染气泡、维护本地未读消息、TCP 收发 | 发送方 A 和接收方 B 的界面与连接 |
| GateServer | HTTP 登录入口 | 登录阶段返回 ChatServer 的地址与 token，不参与每条聊天消息 |
| StatusServer | 选择 ChatServer、签发 token | 让客户端获得可直连的 ChatServer；不参与转发 |
| ChatServer1/2 | TCP 会话、业务校验、在线路由、同服/跨服转发 | 消息收发的核心服务，两个实例代码保持一致 |
| Redis | token、用户在线位置、各 ChatServer 在线人数 | 用 `uip_<uid>` 找到接收方目前在哪台服务器 |
| MySQL | 用户资料、好友、好友申请 | 当前**不保存聊天消息** |
| gRPC / protobuf | ChatServer 之间的内部 RPC | 仅在收发双方落在不同 ChatServer 时使用 |

客户端并不会直接连接 Redis、MySQL 或 gRPC。它只在登录后建立一条到分配的
ChatServer 的 TCP 长连接；服务器主动把收到的新消息推送回该连接。

## 2. 先建立聊天会话：登录链路

聊天前必须先完成 HTTP 登录和 TCP 登录两段流程：HTTP 负责验证密码、选择服务，TCP
负责把“用户身份”绑定到具体长连接。

```text
客户端                         GateServer / StatusServer              ChatServer
  │ HTTP POST /user_login                │                                │
  │ ── 用户名、密码 ────────────────────> │                                │
  │                                      │ 校验账号、选择负载较低的服务   │
  │ <── {uid, host, port, token} ─────── │                                │
  │ TCP connect(host, port)                                               │
  │ ── ID 1005: {uid, token} ───────────────────────────────────────────> │
  │                                      Redis: 校验 utoken_<uid>         │
  │                                      绑定 uid -> CSession             │
  │                                      写入 uip_<uid> = server_name     │
  │ <── ID 1006: 用户资料、好友/申请列表 ─────────────────────────────── │
```

`LogicSystem::LoginHandler` 成功后做了以下几件关键事情：

1. 用 Redis 的 `utoken_<uid>` 校验客户端带来的 token；token 不正确则拒绝 TCP 登录。
2. 加 `login_lock_<uid>` 分布式锁并读取 `uip_<uid>`；同服清理旧会话，跨服同步调用 `NotifyKickUser`，RPC 失败时不覆盖旧路由。
3. 将 `uid` 写入 `CSession`，再通过 `UserMgr` 写入本机内存映射 `uid -> CSession`。
4. 将 Redis 的 `uip_<uid>` 设为本 ChatServer 的配置名；这是跨服定位用户的依据。
5. 用 `HINCRBY login_count <server_name> 1` 维护本机在线人数，供 StatusServer 后续负载均衡。
6. 从 MySQL 加载好友申请和好友列表，随 `ID_CHAT_LOGIN_RSP` 返回，供客户端初始化界面。

连接断开或读写异常时，断线流程先在 `login_lock_<uid>` 内比较 Redis 的
`usession_<uid>`；只有仍指向本 session 才删除 `uip_<uid>` / `usession_<uid>`。
`CServer::ClearSession` 再按 session_id 删除本机映射并将 `login_count` 减一。

> 当前同服清理、跨服 `KickUserReq` 和 `UserMgr` 删除都校验旧 session_id，避免陈旧 RPC 或旧回调误伤新会话。进程崩溃、锁租约过期等生产级边界仍需用 Redis 原子脚本、路由租约和心跳完善。

## 3. 两层协议：客户端 TCP 与服务间 gRPC

### 3.1 客户端与 ChatServer：TCP + JSON

每个 TCP 包的二进制格式固定：

```text
+------------------+------------------+-------------------+
| message id, 2 B   | body length, 2 B | JSON body, N B    |
| BigEndian         | BigEndian        | UTF-8             |
+------------------+------------------+-------------------+
```

| 消息 ID | 方向 | 含义 |
| ---: | --- | --- |
| `1017` `ID_TEXT_CHAT_MSG_REQ` | 客户端 A -> ChatServer | 发起一批文本消息 |
| `1018` `ID_TEXT_CHAT_MSG_RSP` | ChatServer -> 客户端 A | 本次请求的受理/路由结果 |
| `1019` `ID_NOTIFY_TEXT_CHAT_MSG_REQ` | ChatServer -> 客户端 B | 服务端主动推送收到的文本 |

客户端 `TcpMgr::slot_send_data` 用 `QDataStream` 以大端序写入 ID 和长度，随后拼接紧凑
JSON。`readyRead` 先把所有可读字节追加到 `_buffer`，再循环解析完整包；只有读完包头和包体
才调用对应 ID 的 handler，因此能处理半包和一次收到多包的情况。

服务端 `CSession` 则先异步读满 4 字节包头、转为主机字节序后，再循环读满指定长度的包体，
最后封装为 `LogicNode` 投递到 `LogicSystem` 的单独工作线程。发送方向采用 `SendNode` 预先
封包，并用每个会话的发送队列串行 `async_write`，避免多个异步写交叉导致包内容混杂。

服务端拒绝 ID 或 body 长度非法的包。虽然 `MAX_LENGTH` 的注释写作“单包最大长度”，实际
收包代码把 `MAX_LENGTH = 2048` 用作**包体**上限，完整帧连同 4 字节包头最多可达 2052 字节；
客户端虽然可编码 `quint16` 长度，但实际发送内容必须服从这一服务端上限。会话发送队列在
已有待发送节点数大于 `MAX_SENDQUE = 1000` 时会丢弃新的待发送包以保护内存。

### 3.2 ChatServer 与 ChatServer：gRPC + protobuf

跨服文本转发使用 `proto/message.proto` 中的：

```proto
message TextChatData {
    string msgid = 1;
    string msgcontent = 2;
}

message TextChatMsgReq {
    int32 fromuid = 1;
    int32 touid = 2;
    repeated TextChatData textmsgs = 3;
}

message TextChatMsgRsp {
    int32 error = 1;
    int32 fromuid = 2;
    int32 touid = 3;
    repeated TextChatData textmsgs = 4;
}

service ChatService {
    rpc NotifyTextChatMsg(TextChatMsgReq) returns (TextChatMsgRsp);
}
```

这里的 protobuf 不会直接发送给 Qt 客户端。发送方 ChatServer 把 TCP JSON 的
`textArray` 转换为 `TextChatMsgReq`，接收方 ChatServer 再还原为和客户端一致的 JSON，
通过 `1019` 推送。这层转换使客户端协议保持 JSON，同时服务内部得到类型化 RPC 契约。

每台 ChatServer 同时扮演 gRPC 服务端和客户端。`ChatGrpcClient` 按对端服务器名维护
`ChatConPool`（当前每个对端预建 5 个 stub），`NotifyTextChatMsg` 从连接池借出 stub 调用
对端 RPC，并在作用域退出时归还。

## 4. 文本数据设计

### 4.1 客户端请求与通知的 JSON

发送请求 `1017` 和服务端通知 `1019` 使用相同的主体结构：

```json
{
  "fromuid": 9001,
  "touid": 9002,
  "textArray": [
    {
      "content": "你好，今天方便聊一下吗？",
      "msgid": "{a Qt QUuid}"
    }
  ]
}
```

服务端给发送方的 `1018` 回包会在此基础上携带 `error`，并原样带回请求的
`fromuid`、`touid` 和 `textArray`：

```json
{
  "error": 0,
  "fromuid": 9001,
  "touid": 9002,
  "textArray": [{"content": "你好", "msgid": "{uuid}"}]
}
```

字段语义如下：

| 字段 | 类型 | 作用 |
| --- | --- | --- |
| `fromuid` | 整数 | 发送者业务 UID；服务端会与已登录 TCP 会话的 UID 比对 |
| `touid` | 整数 | 接收者业务 UID；用来查 Redis 路由与本机会话 |
| `textArray` | 数组 | 一次请求包含的文本消息集合，按数组顺序转发 |
| `content` | UTF-8 文本 | 真正展示的消息内容 |
| `msgid` | Qt `QUuid` 字符串 | 客户端为每条文本生成的唯一标识，当前用于关联 `1018` 的成功/失败日志 |
| `error` | 整数 | 仅回包/通知使用；`0` 为成功，非 0 为错误码 |

`msgid` 在当前实现中不是数据库主键，也没有服务端去重、重试和已读确认逻辑；它只是一次
发送关联标识。因此同一请求因网络重试重复到达时，当前服务端不会自动幂等去重。

### 4.2 当前持久化数据与未持久化数据

MySQL 已有的数据表与消息关系如下：

| 表 | 已存数据 | 对聊天收发的作用 |
| --- | --- | --- |
| `user` | UID、登录名、昵称、签名、头像等 | 登录/好友展示信息 |
| `user_id` | 业务 UID 分配器 | 注册时分配 UID |
| `friend` | 双向好友关系、单向备注 | 登录加载联系人；客户端收到消息时也用本地好友列表确认发送者 |
| `friend_apply` | 好友申请、备注、处理状态 | 与好友流程有关，不存聊天正文 |

当前 SQL 中没有聊天会话表、聊天消息表，也没有 `INSERT` 聊天记录的代码。因此 `content` 和
`msgid` 只存在于网络包、接收端内存和界面控件中。Redis 也不充当消息队列或离线消息存储：
它只保存短生命周期的 token、用户路由、在线人数和用户资料缓存。

## 5. 发送方客户端 A 的执行过程

入口是 `ChatDialog::slot_send_message`，由发送按钮或输入框 Enter（不按 Shift）触发。

```text
输入框 MessageTextEdit
  -> ChatDialog::slot_send_message
  -> 为每条文本生成 msgid，先创建本人气泡
  -> 组装 textArray，发出 TcpMgr::sig_send_data(1017, JSON)
  -> TcpMgr::slot_send_data 写入 TCP 包
  -> 收到 1018 后按 msgid 打印 sent / failed 日志
```

发送前会检查：本地用户是否已登录、是否选中了聊天对象、以及 `QTcpSocket` 是否仍处于
`ConnectedState`。不满足条件时弹窗提示，且不会发包。

消息编辑器的 `MsgInfo` 支持 `text`、`image`、`file` 三种标志，但目前真正进入网络协议的
只有 `text`：

- 文本：转换为 UTF-8，生成 `QUuid`，加入 `textArray` 并发送。
- 图片：本地创建 `PictureBubble` 预览，但没有图片上传、URL 传输或服务端处理。
- 文件：本地按缩略图展示，同样没有文件传输实现。

一次点击可能带多个文本项。客户端按文本 UTF-8 字节累计到 `1024` 字节时拆分批次；每个
批次发一个 `1017` 包。这个拆分降低了越过服务端 2 KiB 包上限的概率，但计算没有包含 JSON
包装、`msgid` 等额外字节，极端内容仍可能超过服务端限制。

发送气泡在调用 `sig_send_data` 前就已插入界面，属于乐观展示；`1018` 到达后，当前客户端
仅按 `msgid` 输出 `sent` 或 `failed` 调试日志，尚未把气泡改成失败状态、撤回或提供重试按钮。

## 6. ChatServer 的处理、鉴权与路由

`CSession` 收到完整 `1017` 后，将消息送入 `LogicSystem` 队列；回调表把该 ID 分派给
`LogicSystem::HandleTextMsg`。流程如下：

```text
收到 JSON
  -> 解析 fromuid、touid、textArray
  -> 校验 session->GetUserId() == fromuid
  -> Redis GET uip_<touid>
       ├─ 不存在：接收者离线，1018 error = UidInvalid
       ├─ 当前 ChatServer：UserMgr 取 CSession，TCP 1019 推送
       └─ 其他 ChatServer：转 TextChatMsgReq，gRPC NotifyTextChatMsg
  -> 无论中途何种 return，Defer 都向 A 发送 1018
```

服务端对请求的本地校验包括：

1. `fromuid` 必须等于此 TCP 会话登录时保存的 UID，避免客户端伪造别人的发送者 ID。
2. `touid` 必须为正数。
3. `textArray` 必须是非空 JSON 数组。

校验失败返回 `Error_Json (1001)`。目标 UID 没有 Redis 在线路由、路由指向本机但本机
会话不存在，或接收方 gRPC 实现找不到会话时返回 `UidInvalid (1011)`。对端 gRPC 调用失败、
没有对应连接池或没有可用 stub 时，`ChatGrpcClient` 返回 `RPCFaild (1002)`。

> 当前文本处理没有检查 A 与 B 是否已是好友，也没有逐条校验 `content` 或 `msgid` 是否存在、
> 长度是否合理。它只验证会话身份、目标 UID 和数组外形；这些是后续需要补齐的业务校验。

## 7. 同服与跨服投递

### 7.1 A 与 B 在同一 ChatServer

```text
客户端 A                 ChatServer1                         客户端 B
  │ 1017 {A, B, texts}       │                                  │
  │ ──────────────────────> │                                  │
  │                          │ Redis: uip_B == ChatServer1      │
  │                          │ UserMgr[B] -> B 的 CSession       │
  │                          │ ── 1019 {A, B, texts} ──────────> │
  │ <── 1018 {error: 0,...} │                                  │
```

`UserMgr` 是每台 ChatServer 的进程内映射 `uid -> shared_ptr<CSession>`。同服投递无需
RPC，`HandleTextMsg` 直接调用接收方 `CSession::Send`，该会话的发送队列再以异步写推送
给 B。`1018` 通过 `Defer` 在处理函数退出时回给 A。

### 7.2 A 与 B 在不同 ChatServer

```text
客户端 A          ChatServer1               ChatServer2          客户端 B
  │ 1017               │                         │                    │
  │ ─────────────────> │                         │                    │
  │                    │ Redis: uip_B=ChatServer2 │                    │
  │                    │ gRPC NotifyTextChatMsg   │                    │
  │                    │ ───────────────────────> │ UserMgr[B]         │
  │                    │                         │ ── 1019 ─────────> │
  │                    │ <── TextChatMsgRsp       │                    │
  │ <── 1018(error) ── │                         │                    │
```

发送方服务把每个 JSON 元素转换为 `TextChatData(msgid, msgcontent)` 放入 protobuf 的
`repeated textmsgs`。接收方的 `ChatServiceImpl::NotifyTextChatMsg`：

1. 在本机 `UserMgr` 查找 `touid` 的会话；找不到时把 RPC response 的 `error` 设为 `1011`。
2. 将 protobuf 重新组装为 `fromuid`、`touid`、`textArray[{content,msgid}]` 的 JSON。
3. 调用接收方会话的 `Send(..., ID_NOTIFY_TEXT_CHAT_MSG_REQ)`，以 TCP 推送给 B。
4. 返回 gRPC `Status::OK`；业务结果通过 protobuf response 中的 `error` 传回 ChatServer1。

因此要区分两类结果：gRPC `Status` 表示 RPC 网络调用是否成功，response 的 `error` 表示
请求已到达接收方服务后能否找到会话等业务结果。`ChatGrpcClient` 在 RPC `Status` 非 OK 时
覆盖 response 的错误为 `RPCFaild`。

## 8. 接收方客户端 B 的显示与未读处理

`TcpMgr` 收到 `1019` 后解析 JSON：若 `error == 0`，它遍历 `textArray`，将每个元素构造为：

```cpp
TextChatData(msgid, content, fromUid, toUid)
```

再发出 `sig_text_chat`。`ChatDialog::slot_text_chat` 的显示规则是：

1. 在客户端 `UserMgr` 的好友列表中按 `fromUid` 查找发送者；找不到则直接忽略该消息并打印警告。
2. 若当前打开的不是发送者会话，将消息放入 `_unread_text_messages[fromUid]`，更新会话预览并显示红点。
3. 若聊天窗口正是该发送者，立即创建 `ChatRole::Other` 的 `TextBubble` 并插入聊天视图。
4. 用户之后选中该会话时，`SetCurrentChatUser` 取出内存中的未读数组、依次补画气泡、清除红点。

这套未读机制只存在 `ChatDialog` 的内存中：不写数据库、不写 Redis，也没有程序重启恢复。
离线时，发送方会在 `HandleTextMsg` 直接获得 `1011`，接收方下次登录不会收到该条文本。

## 9. 投递语义、错误与当前边界

| 情况 | A 的 `1018` | B 的结果 |
| --- | --- | --- |
| A 未登录会话与 `fromuid` 不符 / 参数错误 | `1001 Error_Json` | 不投递 |
| B 没有 `uip_<uid>` 路由（离线） | `1011 UidInvalid` | 不投递、不存储 |
| B 同服且存在会话 | `0` | 收到 `1019`，客户端显示或暂存未读 |
| B 跨服且 RPC/会话成功 | `0` | 对端 ChatServer 推送 `1019` |
| 对端 RPC 网络失败或连接池不可用 | `1002 RPCFaild` | 不保证投递 |
| Redis 路由仍在但本地目标会话已丢失 | `1011 UidInvalid` | 不投递 |

`1018 error = 0` 的准确含义是：当前 ChatServer 已成功完成本机推送调用，或跨服 RPC 已返回
成功业务结果。它不是以下任何一种保证：

- 消息已写入持久化数据库；
- B 的 Qt 客户端已渲染气泡；
- B 已阅读；
- 网络断线后的消息能补发；
- 发送重试不会造成重复消息。

另外，`CSession::Send` 在发送队列满时会直接丢包且没有把失败回传到业务层。因此在极端慢
接收端场景下，服务端可能已经把 `1018` 视为成功回给 A，但排队中的 `1019` 随后被丢弃；
当前协议没有端到端确认来发现该情况。

## 10. 已预留但尚未接通的能力

协议 ID 已定义，但当前 `LogicSystem` 和 `TcpMgr` 未注册完整处理链路：

| ID | 规划能力 | 当前状态 |
| ---: | --- | --- |
| `1021` | 用户下线通知 | 仅定义 ID |
| `1023/1024` | 心跳请求/回复 | 仅定义 ID |
| `1025/1026` | 加载聊天会话列表 | 仅定义 ID |
| `1027/1028` | 创建私聊会话 | 仅定义 ID |
| `1029/1030` | 加载历史消息 | 仅定义 ID |

要把当前的“在线瞬时转发”升级为可恢复聊天，至少需要补齐：聊天会话与消息表设计、服务端先
持久化再投递、按 `msgid` 的幂等约束、未送达/已送达/已读状态、离线拉取、客户端本地状态
更新和失败重试。届时 `1018` 也应明确表示“已持久化”还是“已投递”，避免把不同层次的确认
混在一个错误码里。

## 11. 关键代码索引

| 位置 | 内容 |
| --- | --- |
| `chat_app desktop/chatdialog.cpp` | `slot_send_message` 组装、乐观渲染与批量发送；`slot_text_chat` 显示/暂存未读 |
| `chat_app desktop/tcpmgr.cpp` | TCP 封包、拆包，`1018`/`1019` 的 JSON handler |
| `chat_app desktop/userdata.h` | 客户端 `TextChatData`、`MsgInfo` 相关的数据对象 |
| `ChatServer1/include/const.h` | 包长度、队列上限、消息 ID、Redis key 前缀 |
| `ChatServer1/src/CSession.cpp` | 服务端读满包头/包体、发送队列与异步写 |
| `ChatServer1/src/LogicSystem.cpp` | `HandleTextMsg` 的身份校验、Redis 路由、同服/跨服分支 |
| `ChatServer1/src/UserMgr.cpp` | 本机 `uid -> CSession` 映射 |
| `ChatServer1/src/ChatGrpcClient.cpp` | 对端 ChatServer 的 gRPC 连接池与调用 |
| `ChatServer1/src/ChatServiceImpl.cpp` | `NotifyTextChatMsg`：跨服请求还原为 TCP 推送 |
| `ChatServer1/src/CServer.cpp` | 断线清理路由与在线人数 |
| `proto/message.proto` | 服务间 `TextChatMsgReq/Rsp` 与 `TextChatData` 契约 |
| `sql/create_tables.sql` | 当前 MySQL 表结构，说明尚无聊天消息表 |

`ChatServer2` 与 `ChatServer1` 的相关实现文件目前哈希一致，以上服务端流程同样适用于
两个实例；它们的差异主要来自各自 `config.ini` 配置的 TCP / gRPC 端口与服务名。
