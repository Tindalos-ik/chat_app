# Qt 样式表（QSS）知识点

[Qt 官方 Style Sheets 文档](https://doc.qt.io/qt-6/stylesheet.html)

> 项目里的实际用法：全局样式文件 `style/stylesheet.qss`（打进 qrc 资源，`main.cpp` 里加载），登录/注册/聊天三个界面共用；动态换肤的核心套路是 **`setProperty("state", ...)` + `repolish`**。
> 对应代码：`clickedlabel.h/cpp`（睁眼闭眼、忘记密码变色）、`statewidget.h/cpp`（侧边栏六状态按钮）、`logindialog.cpp` 的 `showtip`（错误提示变色）。
> 控件提升、UI 文件与信号槽的知识见 [Qt知识点.md](Qt知识点.md)。

QSS 全称 **Qt Style Sheets**，语法基本照抄 CSS（准确说是 CSS 2.1 子集 + Qt 自己的扩展），用来给 Qt 控件换皮肤。它不是一门语言，就是"选择器 + 属性 + 值"的配置文本，写错了不会报错，只是静默不生效。


## 1. 样式表从哪里来：三种设置方式

### 方式一：全局样式文件（本项目）

`main.cpp` 里把 qss 文件读出来设置到 QApplication 上，整个程序所有控件都生效：

```cpp
QFile qss(":/style/stylesheet.qss");
if (qss.open(QFile::ReadOnly)) {
    QString style = QLatin1String(qss.readAll());
    a.setStyleSheet(style);   // 设置到 app = 全局生效
    qss.close();
}
```

知识点：

* qss 文件要先登记到 `resource.qrc` 里，才能用 `:/style/stylesheet.qss` 这种资源路径读（编译时会被打进 exe）；
* `setStyleSheet` 可以调在 `QApplication` 上（全局），也可以调在某个控件上（只对那棵子树生效）。

### 方式二：控件内联样式（局部）

直接在代码里给某个控件写样式，优先级最高：

```cpp
// textbubble.cpp：让 QTextEdit 透明、去边框
m_pTextEdit->setStyleSheet("QTextEdit{background:transparent;border:none}");
```

### 方式三：.ui 文件里写 styleSheet 属性

Designer 里选中控件，在属性栏的 styleSheet 里写，等价于控件内联。本项目里聊天界面的大量样式就是这样写的（搜索框、发送按钮、滚动条）。

> 三种方式的关系：**控件内联 > 全局文件**。全局样式是"默认皮肤"，个别控件想特殊处理就用内联覆盖。


## 2. 选择器语法

| 选择器 | 含义 | 项目例子 |
| --- | --- | --- |
| `*` | 所有控件 | 少用，容易误伤 |
| `QPushButton` | 类型选择器，匹配该类型**及其子类** | `QPushButton:hover` |
| `.QPushButton` | 类选择器，只匹配该类型本身（不含子类） | 少用 |
| `#objectName` | ID/对象名选择器，按 `objectName` 匹配 | `#err_tip`、`#side_chat_lb` |
| `QDialog QLabel` | 后代选择器（中间有空格） | 少用 |
| `QDialog > QLabel` | 直接子控件选择器 | 少用 |
| `[state='normal']` | 属性选择器，匹配动态属性 | 本项目的核心！ |
| `:hover` | 伪状态 | `QPushButton:hover` |

项目里最常见的一句：

```css
QDialog#LoginDialog, #RegisterDialog {
    background-color: rgb(255,255,255);
}
```

知识点：

* `QDialog#LoginDialog` 是"类型 + ID"组合：只有 QDialog 类型的、objectName 叫 LoginDialog 的控件才匹配；
* 逗号 `,` 表示"或"，多个选择器共用一套样式；
* `#RegisterDialog` 没写类型，意思是"只要 objectName 是 RegisterDialog 就行"——如果两个控件同名，两个都会被命中。


## 3. 常用属性：先理解盒模型

每个控件在 QSS 眼里都是一个盒子：

```text
┌────────────── 外边距 margin ──────────────┐
│  ┌────────── 边框 border ──────────┐       │
│  │  ┌────── 内边距 padding ──────┐ │       │
│  │  │                            │ │       │
│  │  │        内容 content         │ │       │
│  │  │                            │ │       │
│  │  └────────────────────────────┘ │       │
│  └──────────────────────────────────┘       │
└────────────────────────────────────────────┘
```

项目里用到的属性：

| 属性 | 作用 | 例子 |
| --- | --- | --- |
| `background-color` | 背景色 | 侧边栏 `#2e2e2e`、登录框白色 |
| `color` | 文字颜色 | 错误提示红/绿 |
| `font-size` / `font-weight` | 字号/字重 | 标题 16px bold |
| `border` / `border-radius` | 边框 / 圆角 | 搜索框圆角 15px、发送按钮圆角 4px |
| `padding` | 内边距 | 搜索框 `5px 10px` |
| `border-image` | 用图片铺满整个控件 | 侧边栏图标按钮 |

### border-image 和 background-image 的区别

* **`border-image`**：把图片**拉伸/九宫格**铺满控件，图标按钮常用——图片会跟着控件尺寸变形，所以图片尺寸要尽量接近控件尺寸；
* **`background-image`**：默认**不缩放**，从左上角平铺/对齐，适合纹理背景。

> 侧边栏按钮就用 border-image 换图标：图片 32×32，按钮 44×44，会轻微放大，肉眼基本无感；如果控件比图片大太多就会糊。


## 4. 动态换肤：本项目最核心的知识点

### 4.1 原理：属性选择器 + repolish

QSS 里可以写**属性选择器**，匹配控件上的任意动态属性：

```css
QLabel#err_tip[state="normal"] {
    color: green;
}

QLabel#err_tip[state="err"] {
    color: red;
}
```

代码里改属性：

```cpp
void LoginDialog::showtip(QString str, QString state) {
    ui->err_tip->setText(str);
    ui->err_tip->setProperty("state", state);   // 改属性
    repolish(ui->err_tip);                      // 刷新样式，关键！
}
```

知识点：

* `setProperty("state", "err")` 只是改了控件的一个属性，**QSS 不会自动重新计算**——必须手动让样式"重刷"一遍；
* 重刷就是 `repolish`：先 `unpolish`（把旧样式清掉）再 `polish`（重新读样式表应用），这样属性选择器才会按新值匹配。

`repolish` 在项目里的定义（global.h 声明 + global.cpp 实现）：

```cpp
// global.h
extern std::function<void(QWidget*)> repolish;

// global.cpp
std::function<void(QWidget*)> repolish = [](QWidget* w) {
    w->style()->unpolish(w);
    w->style()->polish(w);
};
```

### 4.2 实际例子一：睁眼/闭眼图标（ClickedLabel）

注册界面的密码可见性切换按钮，一个 QLabel 用 state 切换两张图：

```css
ClickedLabel#pass_visible[state='unvisible'] {
    border-image: url(:/res/unvisible.png);       /* 闭眼 */
}
ClickedLabel#pass_visible[state='visible'] {
    border-image: url(:/res/visible.png);         /* 睁眼 */
}
```

点击时：

```cpp
_curstate = ClickLbState::Selected;
setProperty("state", _selected_hover);  // 换成睁眼图
repolish(this);                          // 刷新
update();                                // 重绘
```

### 4.3 实际例子二：侧边栏六状态按钮（StateWidget）

侧边栏按钮比睁眼闭眼多了"选中"维度，一共六种组合：normal / hover / pressed / selected / selected_hover / selected_pressed。QSS 里给每个状态配一张图：

```css
StateWidget#side_chat_lb[state='normal']  { border-image: url(:/res/chat_icon.png); }
StateWidget#side_chat_lb[state='hover']   { border-image: url(:/res/chat_icon_hover.png); }
StateWidget#side_chat_lb[state='pressed'] { border-image: url(:/res/chat_icon_press.png); }
StateWidget#side_chat_lb[state='selected'],StateWidget#side_chat_lb[state='selected_hover'] {
    border-image: url(:/res/chat_icon_select_hover.png);
}
StateWidget#side_chat_lb[state='selected_pressed'] {
    border-image: url(:/res/chat_icon_select_press.png);
}
```

StateWidget 内部就是"改属性 + 刷新"这一个动作：

```cpp
void StateWidget::setState(const QString &stateName) {
    setProperty("state", stateName);
    repolish(this);
    update();
}
```


## 5. 伪状态 vs 自定义属性状态

QSS 自带伪状态：`:hover`（悬停）、`:pressed`（按下）、`:checked`（选中）、`:focus`（聚焦）等。升级前的侧边栏按钮就是用这个：

```css
QPushButton:hover { background-color: rgba(255,255,255,36); }
QPushButton:checked { background-color: rgba(255,255,255,72); }
```

搜索框聚焦变绿边框：

```css
QLineEdit:focus { border-color: #07c160; }
```

那为什么 StateWidget 不用伪状态，非要自己搞 `[state='...']`？

* 伪状态是 Qt 内建的，**组合有限**。侧边栏按钮需要表达"选中 + 悬停"、"选中 + 按下"这种组合，`:checked:hover` 虽然也能写，但选中后**永远保持高亮**这件事，伪状态表达得绕；
* 自定义属性想叫什么就叫什么（`normal/err/unvisible/visible`），业务含义清晰，一个属性就能表达整个状态机；
* 关键：伪状态由 Qt 内部维护，属性由你自己的代码维护，逻辑完全可控。

> 一句话：**内建状态用伪状态，自定义状态用属性选择器**。


## 6. 自定义控件要让 QSS 生效：paintEvent 是关键

QWidget 的普通子类（比如 StateWidget）**默认不画样式**——QSS 的 background、border-image 对它无效，因为绘制流程没走 QStyle。解决办法是重写 paintEvent，用 QStyleOption 走一遍标准绘制：

```cpp
void StateWidget::paintEvent(QPaintEvent *event) {
    Q_UNUSED(event);
    QStyleOption opt;
    opt.initFrom(this);              // 把 state 属性、几何信息灌进去
    QPainter p(this);
    style()->drawPrimitive(QStyle::PE_Widget, &opt, &p, this);  // 走标准 QStyle 绘制
}
```

知识点：

* `opt.initFrom(this)` 会读取控件的 `state` 动态属性——所以属性选择器才能工作；
* QLabel、QPushButton 这些内置控件内部已经处理了，不用自己写；
* 忘了写 paintEvent，自定义控件上的 QSS 全部静默失效（不报错！）。


## 7. 子控件选择器：定制滚动条等内部零件

QSS 可以选中控件内部的子零件（sub-control），用 `::` 表示。项目里聊天列表的滚动条就是这样定制的：

```css
QScrollBar:vertical { background: transparent; width: 6px; }
QScrollBar::handle:vertical { background: #c8c9cc; border-radius: 3px; }
QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical { height: 0; }
```

常见子控件：

| 子控件 | 作用 |
| --- | --- |
| `QScrollBar::handle` | 滚动条滑块 |
| `QScrollBar::add-line / sub-line` | 上下箭头按钮 |
| `QComboBox::drop-down` | 下拉箭头区域 |
| `QMenu::item` | 菜单项 |
| `QCheckBox::indicator` | 勾选框 |


## 8. 优先级

同一条规则可能被多个选择器命中，谁生效看优先级：

1. **控件内联 setStyleSheet > 全局 app 级样式**；
2. 选择器越"具体"优先级越高：**ID（#xxx）> 类型（QPushButton）> 通配（*）**，属性选择器、伪状态会增加特异性；
3. 同优先级时，**后写的覆盖先写的**。

```css
/* 两个都匹配时，下面这条赢（同优先级，后写覆盖） */
QLabel#err_tip[state="err"] { color: red; }
```


## 9. 常见坑总结

1. **改了 setProperty 忘了 repolish**——样式纹丝不动，这是动态换肤最容易踩的坑；
2. **自定义 QWidget 子类没写 paintEvent**——QSS 全部失效且不报错；
3. **border-image 拉伸**——图标糊了先检查图片尺寸和控件尺寸是否接近；
4. **objectName 重复**——`#id` 选择器会把同名的全命中，样式互相串；
5. **QSS 语法错误静默忽略**——写了不生效先怀疑拼写/引号，用 `qDebug() << style` 打印出来检查；
6. **编码**——qss 文件保持 UTF-8；C++ 源码里写中文注释要配合 CMake 的 `/utf-8` 编译选项（本项目三个 CMakeLists 都有 `add_compile_options(/utf-8 /wd4819)`）。


## 10. 本项目样式表全景

| 位置 | 内容 | 知识点 |
| --- | --- | --- |
| `style/stylesheet.qss`（全局） | 登录/注册背景、err_tip 红绿、睁眼闭眼、忘记密码变色、侧边栏六状态、红点 | 属性选择器 + border-image |
| `chatdialog.ui`（内联） | 搜索框圆角与 :focus、发送按钮 hover/pressed、滚动条、消息区/输入区分割 | 伪状态、子控件选择器 |
| `logindialog.ui` / `registerdialog.ui`（内联） | 头像圆角、输入框边框 | 局部覆盖全局 |
| `textbubble.cpp`（代码内联） | 气泡里 QTextEdit 透明无边框 | 控件级 setStyleSheet |


## 11. 面试/复习自测

* QSS 有哪三种设置方式？优先级怎么排？
* `QDialog#LoginDialog` 和 `#RegisterDialog` 分别是什么选择器？
* `border-image` 和 `background-image` 有什么区别？
* 为什么 `setProperty("state", ...)` 之后必须 `repolish`？repolish 内部做了什么？
* 伪状态和属性选择器分别适合什么场景？侧边栏按钮为什么用属性选择器？
* 自定义 QWidget 子类为什么 QSS 不生效？怎么修？
* `QScrollBar::handle:vertical` 里的 `::` 是什么意思？
* QSS 写错了会报错吗？怎么排查？
* 图标按钮的图片在控件里变糊了，通常是什么原因？
