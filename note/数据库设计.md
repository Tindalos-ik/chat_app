# 数据库设计（用户数据）

---

## 0. 现状：你现在的数据库长什么样

`chat_app` 三个服务（GateServer / StatusServer / ChatServer）的 `MysqlMgr.cpp` 是同一套代码，目前只用了**一张表**，6 个操作：

| 方法 | SQL | 用途 |
| --- | --- | --- |
| `RegUser` | `INSERT INTO users (name, email, password)` | 注册 |
| `Checkuser` | `SELECT COUNT(*) FROM users WHERE name = ?` | 用户名查重 |
| `Checkemail` | `SELECT COUNT(*) FROM users WHERE email = ?` | 邮箱查重 |
| `isMatch` | `SELECT COUNT(*) FROM users WHERE name = ? AND email = ?` | 找回密码校验 |
| `UpdatePwd` | `UPDATE users SET password = ? WHERE name = ?` | 重置密码 |
| `CheckPwd` | `SELECT uid, name, email, password FROM users WHERE name = ?` | 登录校验 |

隐含的表结构只有：`users(uid, name, email, password)`。

两个和参考项目对不上的地方，落地时要先统一：

1. **表名不一致**：你的代码查 `users`，参考项目是 `user`；
2. **字段名不一致**：你的代码用 `password`，参考项目用 `pwd`。

缺口：没有用户资料（昵称/头像/签名/性别）、没有好友关系表、没有好友申请表、uid 的来源没有确定（靠自增还是独立分配）。

---

## 1. 整体结构

4 张表 + 1 个存储过程，把"用户数据"分成了四个域：

```text
┌──────────────────────────────────────────────────┐
│  user_id（ID分配器）                               │
│    id = 当前已分配的最大uid                          │
└──────────────────────┬───────────────────────────┘
                       │ 注册时 UPDATE+1 取新uid
                       ▼
┌──────────────────────────────────────────────────┐
│  user（账号 + 资料）                                │
│  uid / name / email / pwd / nick / desc / sex / icon │
└──────────────────────┬───────────────────────────┘
                       │ 1对N
                       ▼
┌─────────────────────────────┐   ┌─────────────────────────────┐
│  friend（好友关系）            │   │  friend_apply（好友申请）      │
│  self_id + friend_id + back  │   │  from_uid + to_uid + status │
└─────────────────────────────┘   └─────────────────────────────┘
```

**账号资料归 `user`，ID 归 `user_id`，关系归 `friend`，关系建立前的"握手"归 `friend_apply`**。

---

## 2. user 表：账号 + 资料

```sql
CREATE TABLE `user` (
  `id`    int NOT NULL AUTO_INCREMENT,          -- 物理主键（内部用）
  `uid`   int NOT NULL DEFAULT 0,               -- 业务用户ID（对外用），唯一
  `name`  varchar(255) NOT NULL DEFAULT '',     -- 登录名
  `email` varchar(255) NOT NULL DEFAULT '',     -- 邮箱，唯一
  `pwd`   varchar(255) NOT NULL DEFAULT '',     -- 密码
  `nick`  varchar(255) NOT NULL DEFAULT '',     -- 昵称
  `desc`  varchar(255) NOT NULL DEFAULT '',     -- 个性签名
  `sex`   int NOT NULL DEFAULT 0,               -- 0未知 1男 2女
  `icon`  varchar(255) NOT NULL DEFAULT '',     -- 头像
  PRIMARY KEY (`id`),
  UNIQUE INDEX `uid`(`uid`),
  UNIQUE INDEX `email`(`email`),
  INDEX `name`(`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

字段讲解：

| 字段 | 说明 |
| --- | --- |
| `id` | 自增物理主键，只给数据库内部用 |
| `uid` | 对外业务 ID，登录回包、好友关系都引用它；**唯一索引保证不重复** |
| `name` | 登录名；普通索引，查重靠存储过程（不是唯一索引） |
| `email` | 唯一索引，注册/找回密码的身份凭证 |
| `pwd` | 密码（参考项目存的是客户端异或后的串，见第 9 节安全提醒） |
| `nick` / `desc` / `sex` / `icon` | 用户资料域，登录后展示头像昵称要用，你现在的表里没有 |

设计点：

**为什么 `id` 和 `uid` 分离？**

* `id` 自增只是"行号"，不暴露任何业务信息；
* `uid` 从 `user_id` 表分配，**不连续、不从 1 开始**，别人从 uid 猜不出你的注册量，也防遍历抓用户；
* 将来做分布式时，`uid` 可以直接换成雪花 ID 等分布式 ID，`id` 自增不受影响。

**为什么 `name` 不建唯一索引？**

参考项目的重名检查放在存储过程里（`IF EXISTS`）。唯一索引是数据库层的最终防线，存储过程是业务层的主动检查——两个方案可以同时用，但至少要有一个，否则并发注册会重复。

**为什么用 utf8mb4？**

支持 emoji 和生僻字（utf8 只有 3 字节，存不了 4 字节的 emoji）。聊天类应用必选 utf8mb4。

---

## 3. user_id 表：业务 ID 分配器

```sql
CREATE TABLE `user_id` (
  `id` int NOT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 初始化：从 1000 开始分配
INSERT INTO `user_id` VALUES (1000);
```

用法（注册时）：

```sql
UPDATE `user_id` SET `id` = `id` + 1;       -- 自增（单行 UPDATE 天然加锁）
SELECT `id` INTO @new_id FROM `user_id`;    -- 拿到新 uid
```

设计点：

**为什么不用 `user.id` 的自增当 uid？**

* 自增 ID 连续，暴露注册量，且容易被爬虫遍历；
* 自增由 InnoDB 分配，和业务逻辑耦合；`user_id` 是**显式的分配器**，将来可以替换成"号段模式"或雪花算法而不动业务代码；
* 单行 `UPDATE ... SET id = id + 1` 在 InnoDB 里是行锁，天然串行，**并发注册也不会分到重复 uid**。

代价：多一次 UPDATE + SELECT 的开销，且注册被这个单行锁串行化。对教学项目完全没问题，这也是它配合存储过程出现的原因。

---

## 4. reg_user 存储过程：注册的原子性

```sql
CREATE PROCEDURE `reg_user`(IN `new_name` VARCHAR(255),
                            IN `new_email` VARCHAR(255),
                            IN `new_pwd` VARCHAR(255),
                            OUT `result` INT)
BEGIN
    -- 任何 SQL 异常：回滚整个事务
    DECLARE EXIT HANDLER FOR SQLEXCEPTION
    BEGIN
        ROLLBACK;
        SET result = -1;      -- -1 = 系统错误
    END;

    START TRANSACTION;

    -- 用户名已存在
    IF EXISTS (SELECT 1 FROM `user` WHERE `name` = new_name) THEN
        SET result = 0;       -- 0 = 用户名或邮箱已存在
        COMMIT;
    ELSE
        -- 邮箱已存在
        IF EXISTS (SELECT 1 FROM `user` WHERE `email` = new_email) THEN
            SET result = 0;
            COMMIT;
        ELSE
            -- 分配新 uid
            UPDATE `user_id` SET `id` = `id` + 1;
            SELECT `id` INTO @new_id FROM `user_id`;
            -- 插入用户
            INSERT INTO `user` (`uid`, `name`, `email`, `pwd`)
            VALUES (@new_id, new_name, new_email, new_pwd);
            SET result = @new_id;   -- 成功 = 返回新 uid
            COMMIT;
        END IF;
    END IF;
END
```

返回码语义：

| result | 含义 |
| --- | --- |
| `-1` | SQL 异常，事务回滚 |
| `0` | 用户名或邮箱已存在 |
| `> 0` | 注册成功，返回新分配的 uid |

设计点：

**为什么用存储过程而不是代码里分开执行？**

因为"查重 → 分配 uid → 插入用户"这三步必须是一个**原子操作**。如果

```
SELECT COUNT(*) WHERE name=?    -- 查重
SELECT COUNT(*) WHERE email=?   -- 查重
INSERT INTO users (...)         -- 插入
```

三个请求分开发，并发时两个线程同时通过查重、同时插入，就会出现重复用户名/邮箱。存储过程把三步包进一个事务，配合 `user_id` 的单行锁，**要么全成功、要么全失败**。

**为什么不直接靠唯一索引兜底？**

唯一索引（`UNIQUE(email)`）也能拦住重复，但插入失败抛异常，调用方要处理"是哪个字段冲突"；存储过程在数据库层就把"用户名存在/邮箱存在"区分好了，返回码语义更友好。两者不冲突，可以并存。

> 理解存储过程为什么存在（原子性），再用你自己的代码实现同样的效果（比如先插 `user_id` 拿 uid，再插 user，靠唯一索引兜底），两种路线都值得写一遍。

---

## 5. friend 表：好友关系

```sql
CREATE TABLE `friend` (
  `id`        int UNSIGNED NOT NULL AUTO_INCREMENT,
  `self_id`   int NOT NULL,          -- 我
  `friend_id` int NOT NULL,          -- 我的好友
  `back`      varchar(255) DEFAULT '',  -- 我给这个好友的备注
  PRIMARY KEY (`id`),
  UNIQUE INDEX `self_friend`(`self_id`, `friend_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

参考数据（双向各一行）：

```sql
INSERT INTO `friend` VALUES (55, 1055, 1054, 'sqy');   -- 1055 的好友是 1054，备注 sqy
INSERT INTO `friend` VALUES (56, 1054, 1055, '');      -- 1054 的好友是 1055
```

设计点：

**为什么存双行而不是一行？**

好友是双向关系，但"备注"是**单向的**——我给 A 的备注和 A 给我的备注可以不同。存双行后：

* 查"我的好友列表"：`SELECT * FROM friend WHERE self_id = ?`，一条 SQL 搞定；
* 改备注只改我这一行，不影响对方；
* 删除好友要删两行（`self_id=x AND friend_id=y` + 反向），用事务保证。

**唯一索引 `(self_id, friend_id)` 防什么？**

防止同一个方向重复加好友（1055→1054 出现两次）。它不阻止反向行（1054→1055），所以双行设计不受影响。

---

## 6. friend_apply 表：好友申请

```sql
CREATE TABLE `friend_apply` (
  `id`      bigint NOT NULL AUTO_INCREMENT,
  `from_uid` int NOT NULL,           -- 申请人
  `to_uid`   int NOT NULL,           -- 被申请人
  `status`   smallint NOT NULL DEFAULT 0,  -- 0待处理 1已同意
  PRIMARY KEY (`id`),
  UNIQUE INDEX `from_to_uid`(`from_uid`, `to_uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

设计点：

**为什么申请要落库而不是发完就算？**

申请是一个**可查询的持久状态**：对方离线也能收到申请，同意/拒绝的结果要能追溯，重复申请要能识别。`status` 就是这条申请的状态机：

```
0 待处理 ──同意──► 1 已同意
  │
  └──（可扩展 2 = 拒绝）
```

**唯一索引 `(from_uid, to_uid)` 防什么？**

防止同一个人对同一个人反复刷申请。注意这个唯一约束是"方向性"的，A→B 和 B→A 是两条不同记录，互相追求的场景也能表达。

**同意申请后要做什么（事务）？**

```
START TRANSACTION;
UPDATE friend_apply SET status = 1 WHERE from_uid = ? AND to_uid = ?;
INSERT INTO friend (self_id, friend_id) VALUES (A, B);   -- 双行
INSERT INTO friend (self_id, friend_id) VALUES (B, A);
COMMIT;
```

两个写操作必须原子，否则会出现"申请已同意但好友没加上"的脏状态。

---

## 7. 落地建议：从现状升级到 llfc1

### 7.1 建表顺序

```text
1. user_id      ← 先建（注册依赖它分配 uid）
2. user         ← 再建（依赖 user_id 的 uid）
3. friend       ← 好友关系
4. friend_apply ← 好友申请
```

### 7.2 要改的代码点（对照当前 MysqlMgr）

| 当前方法 | 建议 |
| --- | --- |
| `RegUser` | 改调用 `reg_user` 存储过程（X DevAPI 支持 CALL），或保持直插但加上 `UNIQUE` 索引兜底并发 |
| `Checkuser` / `Checkemail` | 如果走存储过程，这两次 COUNT 就不需要了；不走存储过程则保留 |
| `CheckPwd` | SQL 不变，但 `SELECT` 字段从 `users.password` 改成 `user.pwd`，并补上 `nick/icon/sex` 的查询（登录后要展示） |
| `UpdatePwd` / `isMatch` | 表名/字段名同步统一 |

### 7.3 注意点

* 三份 `MysqlMgr.cpp`（GateServer/StatusServer/ChatServer）是复制出来的，改 SQL 要三处同步改，或抽成公共库；
* 表名统一成一个：要么把代码改成 `user`，要么建表时建 `users`，**别让代码和建表脚本各说各话**；
* `UserInfo` 结构体目前只有 user/email/passwd/uid，加了 nick/icon 后要同步扩展（登录回包、聊天列表都要用）。

---

## 8. 什么该存 MySQL、什么该存 Redis

| 数据 | 存储 | 理由 |
| --- | --- | --- |
| 账号、密码 | MySQL `user` | 强一致、永久保存 |
| 用户资料（昵称/头像/签名） | MySQL `user` | 低频修改、需要 SQL 查询 |
| 好友关系、好友申请 | MySQL `friend` / `friend_apply` | 关系数据，需要事务和查询 |
| 验证码 | Redis（带 TTL） | 自动过期，你已经在这么做 |
| token | Redis（带 TTL） | 高并发读写、可踢下线 |
| 在线状态 | Redis | 秒级变化，MySQL 扛不住写 |
| 未读数 | Redis（INCR） | 高频自增 |

**要永久、要查询、要事务的进 MySQL；要快、要过期、高频写的进 Redis。**

---

## 9. 常见坑汇总

| 坑 | 说明 |
| --- | --- |
| 表名/字段名不一致 | 代码写 `users.password`，参考项目是 `user.pwd`，直接导 llfc.sql 会报表不存在 |
| 并发注册重复 | COUNT 查重 + INSERT 分开执行有竞态；用存储过程或唯一索引兜底 |
| uid 直接用自增主键 | 暴露注册量、可遍历；用 `user_id` 独立分配 |
| 删好友只删一行 | 双行存储必须删两条，用事务 |
| 同意申请不更新状态 | 会出现"好友加了但申请还是待处理" |
| 密码异或不是加密 | `xorString` 可逆，数据库被拖库等于明文泄露；认真做要 bcrypt/argon2 哈希 + salt |
| 字符集不是 utf8mb4 | emoji、生僻字存不进去或变问号 |
| 分布式下建外键 | 多服务直连同一库时，外键影响扩展和分库；关系一致性用应用层 + 事务保证（llfc1 也没建外键） |

---

## 

llfc2 在 llfc1 基础上增加了会话模型（`chat_thread` + `private_chat` + `chat_message`）、群聊（`group_chat` + `group_chat_member`），并把好友申请加上了留言字段。等做到"聊天消息落库、会话列表、群聊"这些功能时，再单独整理一篇，本文只负责用户数据这块的地基。
