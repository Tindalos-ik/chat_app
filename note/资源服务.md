# 资源服务

ResourceServer 使用独立 TCP 长连接处理头像、聊天图片等二进制资源。它的帧格式为
`消息 ID（2 字节） + 包体长度（4 字节） + JSON 包体`；二进制分片放在 JSON 的 Base64
字段中，因此不能直接使用 ChatServer 的文本消息帧传输图片。

## 上传和资源标识

上传任务以 `upload_id` 标识，完成后的文件位于 ResourceServer 配置的上传目录。聊天等
上层业务应保存这个不透明的标识为 `resource_id`，而不是保存 ResourceServer 进程所在机器
的绝对文件路径。

> 当前旧上传回包仍带有 `resource_url`，用于已有头像逻辑兼容；它可能是本机路径，不能作为
> 跨机器聊天图片的下载地址。后续图片消息统一使用 `resource_id`。

## 下载协议

下载是客户端拉取式分片协议。每一个请求只取得一个分片，客户端确认写入本地缓存后再请求
下一片，因此断线时可用已落盘字节数继续下载。

| 消息 ID | 方向 | 含义 |
| ---: | --- | --- |
| `1007` `ID_DOWNLOAD_FILE_REQ` | Qt 客户端 -> ResourceServer | 请求一个资源分片 |
| `1008` `ID_DOWNLOAD_FILE_RSP` | ResourceServer -> Qt 客户端 | 返回一个资源分片或错误 |

请求示例：

```json
{
  "resource_id": "9f0a..._4096_1024",
  "offset": 0,
  "chunk_size": 2048
}
```

成功回包示例：

```json
{
  "error": 0,
  "resource_id": "9f0a..._4096_1024",
  "total_size": 4096,
  "offset": 0,
  "data": "iVBORw0KGgo...",
  "is_last": false,
  "name": "photo.png"
}
```

`data` 是 Base64；`offset` 按原始文件字节计算。成功处理后，下一请求的 offset 为
`offset + Base64 解码后的 data 字节数`。当 `is_last` 为 `true` 时，当前分片正好结束于
`total_size`。服务端只允许读取已完成且元数据与最终文件大小一致的资源，并始终从
`resource_id` 对应的任务目录解析文件，拒绝客户端提供路径。

单片原始数据最大为 2048 字节，为 Base64、JSON 和 ResourceServer 的 4 KB 包体上限预留
空间。`1016`、`1017`、`1018` 分别表示资源不存在、资源尚未完成、文件读取失败。

## 图片聊天的 gRPC 核验

图片聊天不把二进制转发给 ChatServer。客户端先按上传协议拿到 `upload_id`，再以它作为
`resource_id` 发送 1033；ChatServer 在写 `chat_message` 前调用 ResourceServer 的
`ResourceService.VerifyImages`（默认 gRPC 50057）。

核验批次是原子的：只要任意 `resource_id` 不存在、仍在上传、最终文件大小与 `meta.json`
不一致，或者文件不是可识别的 PNG/JPEG/GIF/WebP，整个批次失败且没有任何聊天记录写入。
`1019` 表示“已完成但不是受支持图片”。

成功回包按请求顺序返回 `resource_id`、`name`、`mime_type`、`file_size`、`width`、`height`。
文件名来自发布任务，文件大小重新与最终文件比对，MIME 和尺寸由文件头解析；客户端在 1033
附带的同名字段只为协议完整性保留，ChatServer 不会使用它们。

> 上传、下载与 gRPC 核验共用同一把文件锁。因此核验不会看到 `.part` 文件或完成重命名
> 过程中的瞬时状态；gRPC 也绝不返回资源服务的绝对路径。
