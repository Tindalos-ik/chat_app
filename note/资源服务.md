# 资源服务

ResourceServer 使用独立 TCP 长连接处理头像、聊天图片等二进制资源。它的帧格式为
`消息 ID（2 字节） + 包体长度（4 字节） + JSON 包体`；二进制分片放在 JSON 的 Base64
字段中，因此不能直接使用 ChatServer 的文本消息帧传输图片。

## 上传和资源标识

上传任务以 `upload_id` 标识，完成后的文件位于 ResourceServer 配置的上传目录。聊天等
上层业务应保存这个不透明的标识为 `resource_id`，而不是保存 ResourceServer 进程所在机器
的绝对文件路径。

> 当前旧上传回包仍带有 `resource_url`，用于已有头像逻辑兼容；它可能是本机路径，不能作为
> 跨机器聊天图片的下载地址。后续图片消息统一使用 `resource_id`。

## 下载协议

下载是客户端拉取式分片协议。每一个请求只取得一个分片，客户端确认写入本地缓存后再请求
下一片，因此断线时可用已落盘字节数继续下载。

| 消息 ID | 方向 | 含义 |
| ---: | --- | --- |
| `1007` `ID_DOWNLOAD_FILE_REQ` | Qt 客户端 -> ResourceServer | 请求一个资源分片 |
| `1008` `ID_DOWNLOAD_FILE_RSP` | ResourceServer -> Qt 客户端 | 返回一个资源分片或错误 |

请求示例：

```json
{
  "resource_id": "9f0a..._4096_1024",
  "thread_id": 42,
  "uid": 10001,
  "token": "当前登录 token",
  "offset": 0,
  "chunk_size": 2048
}
```

成功回包示例：

```json
{
  "error": 0,
  "resource_id": "9f0a..._4096_1024",
  "total_size": 4096,
  "offset": 0,
  "data": "iVBORw0KGgo...",
  "is_last": false,
  "name": "photo.png"
}
```

`data` 是 Base64；`offset` 按原始文件字节计算。成功处理后，下一请求的 offset 为
`offset + Base64 解码后的 data 字节数`。当 `is_last` 为 `true` 时，当前分片正好结束于
`total_size`。服务端只允许读取已完成且元数据与最终文件大小一致的资源，并始终从
`resource_id` 对应的任务目录解析文件，拒绝客户端提供路径。每个 `1007` 还必须带当前
登录的 `uid`、`token` 和消息所属的 `thread_id`：ResourceServer 通过 StatusServer
校验 token，再查询 `private_chat` 与 `chat_message`，确认该 uid 是私聊成员且该
`resource_id` 确实由这条私聊中的图片或普通文件消息引用后才读取分片。资源 ID 本身不构成读取凭证。

单片原始数据最大为 2048 字节，为 Base64、JSON 和 ResourceServer 的 4 KB 包体上限预留
空间。`1016`、`1017`、`1018` 分别表示资源不存在、资源尚未完成、文件读取失败；`1020`
表示身份、成员关系或资源归属不满足，客户端不应把它当成普通网络重试；`1021` 表示
StatusServer 或聊天数据库暂不可用，客户端会明确提示并提供重试。

上传单文件上限由 ResourceServer 在 `1005`（同步/创建任务）和每个 `1003`（上传分片）请求中
分别强制为 100 MiB，包含边界值。检查在创建任务目录、写入元数据或打开分片文件之前执行；
超过上限时沿用上传响应 JSON 字段，并以 `1022`（`UploadFileTooLarge`）明确表示文件过大。
因此原有 100 MiB 以内的头像和图片上传流程保持兼容，服务端不依赖客户端界面限制。

## 聊天资源的 gRPC 核验

图片聊天不把二进制转发给 ChatServer。客户端先按上传协议拿到 `upload_id`，再以它作为
`resource_id` 发送 1033；ChatServer 在写 `chat_message` 前调用 ResourceServer 的
`ResourceService.VerifyImages`（默认 gRPC 50057）。普通文件核验由 ResourceServer 的
`ResourceService.VerifyFiles` 提供，按 `ResourceFileVerifyReq(resource_ids)` 接收资源 ID，
成功时通过 `ResourceFileVerifyRsp` 返回有序的 `VerifiedFileResource(resource_id, name,
mime_type, file_size)` 元数据。

`VerifyFiles` 只接受安全字符集内的资源 ID，并检查任务已完成、`meta.json` 中的文件名和大小
有效、最终文件存在且实际大小与元数据一致。普通文件不做图片魔数或格式白名单判断。MIME
由 ResourceServer 根据发布文件名扩展名映射，无法识别时使用 `application/octet-stream`；
文件名、MIME 和大小均由 ResourceServer 返回，调用方不得以客户端同名字段覆盖。

`VerifyImages` 和 `VerifyFiles` 都采用原子批次：只要任意 `resource_id` 不存在、仍在上传或
最终文件大小与 `meta.json` 不一致，整批失败且不返回部分资源。图片核验还要求文件可识别为
PNG/JPEG/GIF/WebP；`1019` 表示“已完成但不是受支持图片”。普通文件不应用该图片格式限制。

图片核验成功回包按请求顺序返回 `resource_id`、`name`、`mime_type`、`file_size`、`width`、`height`；
文件核验返回 `resource_id`、`name`、`mime_type`、`file_size`。文件名来自发布任务，文件大小重新
与最终文件比对；图片 MIME 和尺寸由文件头解析，普通文件 MIME 根据发布文件名扩展名映射。调用方
应使用 ResourceServer 返回的元数据，不能以客户端提供的同名字段覆盖。

> 上传、下载与 gRPC 核验共用同一把文件锁。因此核验不会看到 `.part` 文件或完成重命名
> 过程中的瞬时状态；gRPC 也绝不返回资源服务的绝对路径。

> `VerifyFiles` 对应的 `ResourceFileVerifyReq`、`VerifiedFileResource`、
> `ResourceFileVerifyRsp` 和 `ResourceService.VerifyFiles` 必须先由共享 proto 契约提供；
> ResourceServer 实现依赖这些生成类型，本次不修改 proto。
