# 聊天可靠性与资源安全提升

> 本文记录当前代码中私聊消息、图片资源和客户端缓存的可靠性与安全边界。这里的“送达”只表示已进入实时投递路径；消息是否展示、是否已读，以及资源是否允许下载，分别由独立状态和校验链路决定。

## 1. 为什么要拆分消息状态

一条消息从发送到被阅读至少会经过三个不同阶段：服务端持久化、实时投递、接收端展示。把它们合并成一个 `delivered` 字段会让发送方无法判断“对端暂时离线”与“消息已经丢失”，也会把展示误当作阅读。

```text
发送方 Qt
  -> ChatServer 写入 chat_message
  -> 发送确认：persisted=true
  -> 同服 TCP 或跨服 gRPC 实时通知：realtime_delivered=true/false
  -> 接收方气泡真正进入当前聊天视图
  -> 1036 展示 ACK -> displayed_at -> 1037 通知发送方
  -> 接收方进入会话并提交已读游标
  -> 1038 已读回执 -> status=1 -> 1039 通知发送方
```

| 字段或状态 | 事实含义 | 不代表什么 |
| --- | --- | --- |
| `persisted=true` | 消息已提交到 MySQL，可通过历史同步恢复 | 对端在线或已经看到 |
| `realtime_delivered=true` | 已排入同服 TCP 或跨服 gRPC 的实时路径 | 对端 UI 已经渲染 |
| `peer_displayed=true` / `displayed_at` | 接收端已将消息绘制在当前聊天窗口 | 对端已阅读内容 |
| `status=1` | 接收端提交了会话已读游标 | 发送端当前在线并收到通知 |

## 2. 展示 ACK、已读与未读数

`1036` 只在文本气泡实际插入聊天窗口、或图片下载完成并插入 `PictureBubble` 后发送。ChatServer 使用当前 TCP 会话的 uid，而不是信任 JSON 中的用户身份；MySQL 同时限定 `thread_id`、`recv_id`、私聊成员关系和尚未设置的 `displayed_at`。因此客户端不能替其他用户确认展示，重复 ACK 也不会反复通知发送端。

`1038` 使用 `read_through_message_id` 提交会话级已读游标。服务端只更新“当前登录用户是接收者、且仍为未读”的记录；它会先读取本次真正发生状态提升的消息，再更新 `status`，随后才发送 `1039`。这样重试相同游标不会生成重复已读通知。

客户端 SQLite 以 `message_id` 去重，把 `delivery_state` 单调提升为已保存、实时投递、已展示和已读。自己发送的气泡在原“发送失败”图标位置显示 `unread.png`；收到服务端已读状态或 `1039` 后切换为 `readed.png`，悬浮提示保留前三阶段的细分状态。实时包迟到、客户端重启或登录后的历史重放都不能把较高状态回退；会话摘要的 `unread_count` 对新入站消息累加，在进入当前会话后清零并提交已读游标。

> 实时通知属于加速路径，不是状态真相。Redis 没有发送端路由、发送端离线或跨服 RPC 失败时，`displayed_at` 与 `status` 已保存在 MySQL；发送端下次 1030 同步仍能恢复最终状态。

## 3. 历史同步的帧大小边界

ChatServer 的 `1030` 历史响应同时受 `page_size` 和实际紧凑 JSON 序列化字节数约束，单帧上限为 4 KiB。服务端逐条试装消息，以最终会调用的紧凑 JSON 序列化结果测量，而不是按消息条数或图片元数据长度估算。

当下一条消息放不下时，服务端返回已经装入的部分、最后一条的 `next_message_id` 与 `load_more=true`；客户端从该游标继续拉取。若单条消息自身已超过上限，响应 `MessageTooLarge (1016)`，不截断消息、不跳过游标，也不让客户端循环请求同一条记录。

## 4. 图片下载的身份与归属校验

ResourceServer 的下载请求 `1007` 除 `resource_id`、`offset` 和 `chunk_size` 外，必须携带当前 `uid`、登录 `token` 和图片所属 `thread_id`。

1. ResourceServer 调用 StatusServer 校验 `uid + token`。
2. 认证成功后，查询 `private_chat` 和 `chat_message`，确认该用户是目标私聊成员，且该资源确实是该会话已写入图片消息的 `resource_id`。
3. 只有两项都成立才会取得文件锁、解析资源目录并读取分片。

资源 ID 不是下载凭证；即使用户持有有效 token，也不能把另一段会话的 `resource_id` 作为自己的资源下载。认证失败、成员关系不符或资源归属不符返回 `ResourceAccessDenied (1020)`；StatusServer 或聊天库不可用返回 `ResourceAuthorizationUnavailable (1021)`，默认拒绝读取而不是降级放行。下载日志只记录资源 ID、uid、会话和分片位置，不记录 token 或 Base64 正文。

客户端把鉴权拒绝与可恢复故障区分显示：前者不应盲目重试；后者保留下载任务和已落盘偏移，提供“重试/取消”入口。分片协议从已经落盘的偏移继续，因此一次临时网络失败不需要重传完整图片。

## 5. 持久化、实时投递与跨服职责

文本与图片都先写入 `chat_message`，再根据 Redis 用户路由进行同服 TCP 或跨服 gRPC 通知。图片二进制不经过 ChatServer：客户端上传到 ResourceServer，ChatServer 通过 `VerifyImages` 重新取得文件类型、大小、尺寸等可信元数据后才持久化图片消息。

跨服 `NotifyMessageDisplayed` 和 `NotifyThreadRead` 只把**已经写入来源 ChatServer 数据库**的回执推给本机在线发送端，不会按照 RPC 参数二次修改数据库。发送端离线时 gRPC 仍可正常返回，避免来源端把“对端不在线”误判成回执持久化失败而重试写库。

## 6. 涉及范围

| 范围 | 关键实现 |
| --- | --- |
| 状态确认、历史分包、跨服转发 | `ChatServer1/src/LogicSystem.cpp` |
| 展示/已读持久化和幂等筛选 | `ChatServer1/src/MysqlMgr.cpp` |
| 跨服通知接收 | `ChatServer1/src/ChatServiceImpl.cpp`、`ChatGrpcClient.cpp` |
| 客户端 SQLite 状态、未读数、图片重试 | `chat_app desktop/` 下的 `LocalChatStorageMgr`、`ChatDialog`、`ChatImageTransfer`、`ResourceClient` |
| 1007 鉴权与敏感日志保护 | `ResourceServer/src/LogicWorker.cpp`、`CSession.cpp`、`ResourceAccessAuthorizer.cpp` |
| 协议与表结构 | `proto/message.proto`、`sql/chat_message_receipt_migration.sql`、`sql/client_chat_cache.sql` |

