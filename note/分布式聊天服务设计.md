# 分布式聊天服务设计

> 为什么要做分布式：一个 TCP 聊天服务器能扛的连接和并发有限，扛不住就要加机器；一旦有了多台机器，就冒出"客户端该连哪台、某个用户在哪台、消息怎么转发"这些新问题。这篇把整个项目为什么拆服务、服务怎么协作、分布式问题分别怎么解决串起来讲清楚。

先记住一句话：**客户端只认识网关（GateServer），聊天服务器（ChatServer）可以有很多台，登录时由状态服务（StatusServer）挑一台并把"一次性通行证"（token）发给客户端，客户端凭 token 直连聊天服务器；用户分散在不同服务器上，服务器之间用 gRPC 互相转发消息。**

## 1. 谁是谁

| 角色 | 实现 | 对外端口 | 一句话职责 |
| --- | --- | --- | --- |
| 客户端 | Qt 桌面端 | - | 只发两种请求：HTTP 短连接（注册/登录/验证码）、TCP 长连接（聊天），不知道服务器内部结构 |
| GateServer | C++ / Beast | HTTP 8080 | 网关，客户端唯一入口；查 MySQL 验密码；把验证码、选服请求转给后面的服务 |
| VarifyServer | Node.js / gRPC | 50051 | 生成邮箱验证码，存 Redis（180 秒过期）并发邮件 |
| StatusServer | C++ / gRPC | 50052 | "路由器 + 发证机关"：登录时选一台负载最小的 ChatServer，签发 token |
| ChatServer1/2 | C++ / Asio | TCP 8090/8091；服务间 gRPC 50055/50056 | 持有客户端 TCP 长连接，维护本机在线用户，跨服消息转发 |
| MySQL | - | 33060（X DevAPI） | 持久数据：账号、好友关系、好友申请、聊天记录（chat_app_db） |
| Redis | - | 6379 | 瞬态数据：验证码、token、用户资料缓存、在线位置、在线人数 |

启动顺序：MySQL → Redis → VarifyServer → StatusServer → ChatServer1/2 → GateServer。

## 2. 为什么要拆成这么多服务

单机聊天服务器的问题：

* 容量有限：所有长连接、消息转发、业务逻辑都在一台机器上，连接数、内存、带宽很快到顶
* 挂了全站不可用：没有冗余，一台机器宕机整个聊天室瘫痪
* 不好扩展：想加机器只能改客户端的服务器地址，每台机器还是各干各的

所以按两个方向拆：

1. 纵向（按职责拆分）：网关、状态服务、聊天服务器、验证码服务各是独立进程，语言各取所长——Node.js 做邮件生态顺手，C++ 做高并发网络服务
2. 横向（同职责多实例）：ChatServer 开两台，一台不够再加一台，客户端完全不用改

## 3. 服务之间怎么通信

| 通信段 | 方式 | 为什么 |
| --- | --- | --- |
| 客户端 → GateServer | HTTP + JSON 短连接 | 注册/登录/验证码是低频请求，一问一答，短连接简单够用 |
| 客户端 → ChatServer | TCP 长连接 | 聊天高频且服务端要主动推送（对方发消息不能等客户端来问），需要长连接 |
| 服务 ↔ 服务 | gRPC | 内部调用要跨语言（C++ 调 Node.js）、要性能，gRPC = HTTP/2 + protobuf 二进制 |

gRPC 最大的好处是"接口即契约"：所有接口写在一份 `proto/message.proto` 里，C++ 和 Node.js 都从这一份生成代码，改协议只改一处，两端不会悄悄不一致。

## 4. 核心流程：注册与登录

### 4.1 注册（验证码怎么用）

客户端 → GateServer(/get_varifycode) → gRPC 调 VarifyServer → 生成验证码存 Redis `code_<邮箱>`（180 秒过期）→ 发邮件。注册时 GateServer 再查一遍 MySQL（邮箱/用户名是否重复）并比对 Redis 里的验证码，通过才写库。

### 4.2 登录（重点：两个通道、两种凭证）

```text
客户端                GateServer                StatusServer              ChatServer
  │ ① POST /user_login   │                         │                        │
  │─────────────────────>│ ② 查MySQL：密码对吗？    │                        │
  │                      │ ③ gRPC GetChatServer    │                        │
  │                      │────────────────────────>│ ④ 选负载最小的那台      │
  │                      │                         │   生成token            │
  │                      │ ⑤ 返回{host,port,token} │   存Redis utoken_uid   │
  │ ⑥ TCP 连host:port    │                         │                        │
  │──MSG_CHAT_LOGIN──────┼─────────────────────────┼───────────────────────>│ ⑦ 校验token
  │<─MSG_CHAT_LOGIN_RSP──┼─────────────────────────┼────────────────────────│ ⑧ 回用户资料
```

具体步骤：

1. 客户端把 email + 密码（xorString 混淆后）POST 给 GateServer
2. GateServer 查 MySQL，确认用户存在、密码正确——这一层叫"验身份"
3. 通过后 gRPC 调 StatusServer 的 `GetChatServer(uid)`
4. StatusServer 选一台负载最小的 ChatServer（负载相同就轮询），生成一次性 token，存 Redis `utoken_<uid>`，把 `{host, port, token}` 返回给客户端
5. 客户端拿地址建 TCP 长连接，发 `MSG_CHAT_LOGIN {uid, token}`
6. ChatServer 校验 token——这一层叫"验凭证"
7. 校验通过后加载用户资料：先查 Redis 缓存 `ubaseinfo_<uid>`，没有就去 MySQL 查并回填缓存
8. 回 `MSG_CHAT_LOGIN_RSP`，客户端进聊天界面；同时服务器做好绑定

为什么 TCP 登录还要 token？TCP 是一条全新通道，客户端只说一句"我是 uid 1002"，谁都能冒充。token 是 HTTP 通道验证过身份的"一次性通行证"，把信任从 HTTP 转移给 TCP，而且 TCP 上永远不出现密码。三个概念分开记：**uid = 你是谁，token = 通行证，host/port = 去哪里**。

登录成功后 ChatServer 做的事（绑定 + 上报）：

* `session->SetUserId(uid)`：会话记住用户身份
* `UserMgr::SetUserSession(uid, session)`：本机内存表 uid → 会话
* Redis `uip_<uid>` = 本服务器名：告诉全世界"这个用户现在在我这里"
* Redis `login_count` 哈希里本服务器计数 +1（`HINCRBY`）：给负载均衡提供数据

## 5. 分布式带来的问题，分别怎么解决

### 5.1 客户端怎么知道该连哪台？

答：客户端永远只连 GateServer。真正的"寻址"发生在服务端：GateServer 问 StatusServer，StatusServer 告诉它哪台 ChatServer 的地址。好处是内部机器随便加、随便换，客户端无感知，内网地址也不用暴露给公网。

### 5.2 怎么保证选到的是"不太忙"的那台？

每台 ChatServer 登录时把 Redis 哈希 `login_count` 里自己的在线数 +1，断开时 -1；StatusServer 选服时读一遍各家计数，取最小；都相同（比如刚启动全是 0）就轮询，避免永远偏向第一台。比纯轮询/随机多了一层"真实负载"感知。

### 5.3 用户散在不同服务器，怎么知道他在哪？

每台 ChatServer 只认识连到自己上的用户，所以"全局答案"放在 Redis：

* `uip_<uid>` = 该用户当前所在的 ChatServer 名
* 本机则维护 `UserMgr`：uid → CSession（`CServer` 里的 `_sessions` 是 session_id → 会话，两张表用途不同，断线时都要清理）

要发消息先查目标 uid 在哪个服务器：在本机就直接从 UserMgr 拿会话推送；在别的机器就走 gRPC 转发过去。

### 5.4 聊天双方不在同一台服务器，消息怎么到？

两台 ChatServer 互相都是"gRPC 服务端 + gRPC 客户端"：各自注册 `ChatService`（监听自己的 rpcport），又按配置文件给对端建好 stub 连接池。发消息链路：

```text
用户A → 所在ChatServer1 → gRPC NotifyTextChatMsg(touid) → 目标ChatServer2 → 找到B的session → TCP推给B
```

好友申请、加好友回执、消息推送都有对应的 RPC 方法，靠消息里的 `fromuid/touid` 说明"这事涉及你机器上的哪个用户"。

### 5.5 同一个账号两台设备登录怎么办？

目标设计（参考 llfcchat / login.md）：登录时用 Redis 分布式锁（`lock_<uid>`）防并发竞争，查 `uip_<uid>` 看旧登录在哪台：

* 同一台：直接找旧 session 通知下线
* 不同台：通过 gRPC 通知那台服务器踢人

保证一个账号同一时刻只在一个连接上。**当前代码还没接这一步**，现在是先登录的先在线、后登录的也能挤上来，后面做了分布式踢人才补上。

### 5.6 数据放 MySQL 还是 Redis？

| 数据 | 存储 | 理由 |
| --- | --- | --- |
| 账号密码、用户资料、好友关系、好友申请 | MySQL | 要永久保存、要查询、要事务 |
| 验证码、token、在线位置、在线人数、用户资料缓存 | Redis | 高频、短生命周期、要过期 |

