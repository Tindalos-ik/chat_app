# 聊天服务器服务

本文只整理 `proto/message.proto` 定义的聊天服务器协议。协议使用
`proto3`，包名为 `message`，由各个 ChatServer 通过 gRPC 互相调用。

## 1. 服务定位

ChatServer 同时承担两种角色：

1. 对外维护客户端的 TCP 长连接，负责把业务消息推送给本机在线用户。
2. 对内作为 gRPC 客户端和服务端，在用户分布到不同 ChatServer 时转发好友和聊天业务。

客户端登录前需要通过 `StatusService.GetChatServer` 获得 ChatServer 地址和 token；
客户端使用返回的 `host`、`port` 连接 TCP ChatServer。`StatusService.Login` 用于
ChatServer 校验 `uid` 与 token 是否匹配。上述接口是 proto 中定义的登录协作接口，
真正的跨 ChatServer 业务转发由下面的 `ChatService` 完成。

## 2. ChatService 接口

```protobuf
service ChatService {
    rpc NotifyAddFriend (AddFriendReq) returns (AddFriendRsp) {}
    rpc RplyAddFriend (RplyFriendReq) returns (RplyFriendRsp) {}
    rpc SendChatMsg (SendChatMsgReq) returns (SendChatMsgRsp) {}
    rpc NotifyAuthFriend (AuthFriendReq) returns (AuthFriendRsp) {}
    rpc NotifyTextChatMsg (TextChatMsgReq) returns (TextChatMsgRsp) {}
}
```

调用方先根据目标用户所在的 ChatServer 选择对端，再通过该服务的 gRPC stub 发起
同步 RPC。对端收到请求后，在本机查找目标用户的会话，并将结果通过 TCP 推送；
处理结果通过 response 返回给调用方。

## 3. 服务调用情形：跨服发送文本消息

假设用户 A 登录在 `ChatServer1`，用户 B 登录在 `ChatServer2`，A 给 B 发送一条
文本消息。一次完整调用如下：

```text
客户端 A
  │ TCP：发送 fromuid、touid 和文本内容
  ▼
ChatServer1
  │ 查询 Redis 中 touid 对应的在线服务器，发现 B 在 ChatServer2
  │ gRPC ChatService.NotifyTextChatMsg(TextChatMsgReq)
  ▼
ChatServer2
  │ UserMgr 根据 touid 查找 B 的 CSession
  │ 通过 B 的 TCP 长连接推送 textmsgs
  │ 返回 TextChatMsgRsp(error、fromuid、touid)
  ▼
ChatServer1
  │ 根据 response 的 error 判断跨服转发结果
  ▼
客户端 A
  └ TCP：收到发送结果
```

`TextChatMsgReq` 中的 `fromuid` 和 `touid` 标识通信双方，`textmsgs` 携带一批
`TextChatData`；每条数据包含 `msgid` 和 `msgcontent`。如果 B 与 A 在同一台
ChatServer，则跳过 gRPC，直接由该服务器通过本地 `UserMgr` 找到 B 的会话并推送。
跨服调用失败属于 gRPC 网络层错误或 response 中的业务错误，不能把“已发起 RPC”
直接当作“消息已送达”。

## 4. 好友申请：NotifyAddFriend

### 请求 `AddFriendReq`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `applyuid` | `int32` | 发起好友申请的用户 ID |
| `name` | `string` | 申请用户昵称，用于展示申请通知 |
| `desc` | `string` | 好友申请附言 |
| `touid` | `int32` | 被申请用户 ID |

### 响应 `AddFriendRsp`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `error` | `int32` | 业务结果，`0` 表示成功 |
| `applyuid` | `int32` | 申请用户 ID，用于匹配本次申请 |
| `name` | `string` | 申请用户昵称，用于通知接收方 |

### 实现流程

申请方所在 ChatServer 根据 `touid` 找到目标用户所在的 ChatServer，调用对端的
`NotifyAddFriend`。接收方服务器使用 `touid` 找到本机用户会话，将申请信息推送给
该用户，并在 response 中返回处理结果。

## 5. 好友申请回复：RplyAddFriend

### 请求 `RplyFriendReq`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `rplyuid` | `int32` | 回复好友申请的用户 ID |
| `agree` | `bool` | 是否同意好友申请 |
| `touid` | `int32` | 原申请用户 ID |

### 响应 `RplyFriendRsp`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `error` | `int32` | 业务结果，`0` 表示成功 |
| `rplyuid` | `int32` | 回复用户 ID |
| `touid` | `int32` | 原好友申请的申请用户 ID |

### 实现流程

回复方所在 ChatServer 根据 `touid` 找到申请方所在的 ChatServer，调用
`RplyAddFriend` 转发 `agree` 和双方 ID。申请方服务器再把同意或拒绝结果通知给
申请用户。请求和响应中的 `touid` 都是原申请用户 ID，使用 `int32` 保持一致。

## 6. 单条聊天消息：SendChatMsg

### 请求 `SendChatMsgReq`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `fromuid` | `int32` | 消息发送者 ID |
| `touid` | `int32` | 消息接收者 ID |
| `message` | `string` | 聊天消息内容 |

### 响应 `SendChatMsgRsp`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `error` | `int32` | 业务结果，`0` 表示成功 |
| `fromuid` | `int32` | 消息发送者 ID |
| `touid` | `int32` | 消息接收者 ID |

### 实现流程

发送者所在 ChatServer 根据 `touid` 定位接收方所在服务器：本机用户直接查找会话，
跨服用户调用对端 `SendChatMsg`。接收方服务器找到用户后，通过客户端 TCP 连接
推送单条消息，并在 response 返回发送方和接收方 ID 及业务错误码。

## 7. 好友认证通知：NotifyAuthFriend

### 请求 `AuthFriendReq`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `uid` | `int32` | 处理好友申请的用户 ID |
| `touid` | `int32` | 原好友申请的申请用户 ID |

### 响应 `AuthFriendRsp`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `error` | `int32` | 业务结果，`0` 表示成功 |
| `uid` | `int32` | 处理申请的用户 ID |
| `touid` | `int32` | 原申请用户 ID |

### 实现流程

处理申请的一方完成好友关系处理后，根据 `touid` 找到原申请用户所在的 ChatServer，
调用 `NotifyAuthFriend`。对端服务器把认证结果通知给原申请用户，双方服务器使用
response 中的 ID 对应本次业务。

## 8. 批量文本消息：NotifyTextChatMsg

### 请求 `TextChatMsgReq`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `fromuid` | `int32` | 消息发送者 ID |
| `touid` | `int32` | 消息接收者 ID |
| `textmsgs` | `repeated TextChatData` | 待推送的文本消息列表 |

`TextChatData` 的字段如下：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `msgid` | `string` | 消息唯一标识 |
| `msgcontent` | `string` | 文本消息内容 |

### 响应 `TextChatMsgRsp`

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `error` | `int32` | 业务结果，`0` 表示成功 |
| `fromuid` | `int32` | 消息发送者 ID |
| `touid` | `int32` | 消息接收者 ID |

### 实现流程

发送者所在 ChatServer 将一批 `TextChatData` 按目标用户所在服务器转发，接收方
服务器找到 `touid` 的会话后逐条推送文本消息。`msgid` 用于客户端或业务层识别
每条消息，`msgcontent` 保存实际文本内容；服务端转发时应完整保留列表顺序和字段值。

## 9. 参考实现中的落地方式

参考项目 `llfcchat` 将每个 ChatServer 做成“TCP 服务端 + gRPC 服务端 + gRPC 客户端”
的双重角色。当前项目可以按下面的结构实现。

### 8.1 启动两个监听端口

ChatServer 启动时创建 Asio TCP 服务器，监听客户端长连接端口；同时使用
`grpc::ServerBuilder` 注册 `ChatServiceImpl`，监听服务间 RPC 端口，并在独立线程中
调用 `Wait()`。收到退出信号时同时停止 Asio、关闭 gRPC server，最后清理在线人数。

```cpp
ChatServiceImpl service;
grpc::ServerBuilder builder;
builder.AddListeningPort(rpc_host + ":" + rpc_port,
                         grpc::InsecureServerCredentials());
builder.RegisterService(&service);
auto grpc_server = builder.BuildAndStart();
std::thread grpc_thread([&] { grpc_server->Wait(); });
```

TCP 端口和 RPC 端口用途不同：前者给客户端连接，后者只给其他 ChatServer 调用，
配置时不能混用。

### 8.2 跨服 gRPC 客户端连接池

参考实现从 `[PeerServer]` 配置读取对端服务器列表，为每个对端建立多个
`ChatService::Stub`，保存为 `server_name -> ChatConPool`。发送跨服消息时：

1. 根据目标用户的在线路由得到对端服务器名称。
2. 从连接池取出一个 stub，创建 `ClientContext`，调用对应 RPC。
3. 使用 RAII 或 `Defer` 将 stub 放回连接池。
4. 检查 gRPC `Status`；网络失败时在 response 的 `error` 写入项目约定的失败码。

连接池停止后，取连接应返回空指针，调用方需要先判断再使用，避免服务关闭阶段崩溃。

### 8.3 服务端处理的共同步骤

`ChatServiceImpl` 的各个通知方法都遵循相同模式：

```text
读取 request 中的 touid
    -> UserMgr 查找本机 uid 对应的 CSession
    -> 找不到：填写 response 后返回 Status::OK
    -> 找到：把 request 字段转换为客户端 TCP 协议的数据
             通过 CSession 推送给目标用户
             填写 response 并返回 Status::OK
```

参考项目用 `UserMgr::GetSession(touid)` 判断目标用户是否在线，用 `CSession::Send`
发送通知。客户端 TCP 消息的 JSON 字段和消息 ID 不属于 `message.proto`，因此本协议
层只要求完整读取和转发 proto 字段，不在此处规定 TCP 封包格式。

### 8.4 本机与跨服路由

用户登录 ChatServer 后，服务端把 `uid -> ChatServer 名称` 写入 Redis，同时在本机
`UserMgr` 保存 `uid -> CSession`。处理好友或聊天请求时先查询该路由：

```text
目标服务器 = Redis 中 uid 的在线服务器
目标服务器是当前服务器 -> UserMgr 找 session，直接 TCP 推送
目标服务器是其他服务器 -> ChatGrpcClient 调用对应 ChatService RPC
```

跨服 RPC 的请求和响应只携带 proto 中声明的数据；用户是否在线由接收方服务器的
本地 `UserMgr` 决定。目标用户不在线时，接收方仍应返回可识别的业务结果，离线消息
持久化不属于当前 `ChatService` proto 的定义。

## 10. 统一处理约定

### 网络状态与业务错误

gRPC 方法返回值是 gRPC `Status`，response 中的 `error` 是业务错误码，两者含义不同：

- gRPC 调用失败、连接不可用或超时，应通过非正常 `Status` 表示网络层失败。
- 请求已到达对端但业务处理失败，应保持 gRPC 调用成功，并在 response 的 `error`
  中返回业务错误码。
- proto 注释约定 `error = 0` 表示业务成功；具体非零错误码由项目现有错误码定义。

### 字段兼容性

`message.proto` 是服务间的共同契约。字段编号（例如 `fromuid = 1`）和字段类型必须
保持不变；新增字段只能使用新的编号。C++、Node.js 等调用方都应由同一份 proto 生成
代码或加载同一份 proto 文件，避免序列化不兼容。

### 本机与跨服

每个 ChatServer 都可以注册并实现 `ChatService`，同时为其他 ChatServer 创建 gRPC
客户端 stub。目标用户在本机时直接操作本地会话；目标用户在其他服务器时调用对应
RPC。ChatService 只定义服务间传输的数据和方法，客户端 TCP 消息格式不属于本文件，
应按客户端与 ChatServer 的另一套消息协议处理。
