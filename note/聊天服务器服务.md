# 聊天服务器服务

本文说明 `proto/message.proto` 中的 `ChatService`，以及当前两个 ChatServer 的实际
实现。它只覆盖 ChatServer 之间的 gRPC 通信；客户端与 ChatServer 的 TCP 消息 ID、
JSON 封包和登录流程见 [消息ID全链路.md](消息ID全链路.md) 与
[登录全链路](login.md)。

> `message.proto` 是所有服务共享的序列化契约，但 proto 中声明的方法不等于都会被业务
> 调用。本文分别记录协议声明和实际调用路径，改动时应以源码和 proto 为准。

## 1. 服务定位

每个 ChatServer 同时承担三项工作：

1. 为 Qt 客户端维护 TCP 长连接，并通过 `CSession::Send` 推送业务通知。
2. 作为 gRPC 服务端，接收其他 ChatServer 转来的业务请求。
3. 作为 gRPC 客户端，根据 Redis 中的在线路由，调用目标 ChatServer。

客户端先经 StatusServer 取得 ChatServer 的 `host`、TCP `port` 和登录 `token`，再建
立 TCP 连接。登录成功后，ChatServer 会保存两份在线状态：

```text
Redis：uip_<uid> -> ChatServer 名称
内存 ：UserMgr 中 uid -> CSession
```

Redis 用于判断消息要路由到哪台服务器，目标服务器的 `UserMgr` 才能确认目标会话仍在
本机内存中。两者不一致时，不能把 Redis 路由存在视为消息已经送达。

## 2. 服务清单与调用情况

`ChatService` 的完整定义如下：

```protobuf
service ChatService {
    rpc NotifyAddFriend (AddFriendReq) returns (AddFriendRsp) {}
    rpc SendChatMsg (SendChatMsgReq) returns (SendChatMsgRsp) {}
    rpc NotifyAuthFriend (AuthFriendReq) returns (AuthFriendRsp) {}
    rpc NotifyTextChatMsg (TextChatMsgReq) returns (TextChatMsgRsp) {}
    rpc NotifyKickUser (KickUserReq) returns (KickUserRsp) {}
}
```

| 方法 | 方向与用途 | 调用情况 |
| --- | --- | --- |
| `NotifyAddFriend` | 申请方所在服务器通知目标用户所在服务器 | 好友申请跨服通知使用 |
| `SendChatMsg` | 转发单条字符串聊天消息 | 仅 proto 声明，未实现 |
| `NotifyAuthFriend` | 认证完成后通知原申请方 | 好友认证跨服通知使用 |
| `NotifyTextChatMsg` | 转发一批文本消息 | 文本消息跨服转发使用 |
| `NotifyKickUser` | 通知旧服踢掉同 uid 的旧会话 | 跨服重复登录使用 |

`ChatServiceImpl` 与 `ChatGrpcClient` 已覆盖上表四个实际业务方法。`SendChatMsg` 是遗留的单条文本接口，当前统一使用 `NotifyTextChatMsg`，直接调用前者会得到 gRPC 未实现结果。

## 3. 路由与端口

ChatServer 启动时会同时监听两个端口：TCP 端口面向客户端，gRPC 端口面向其他
ChatServer。`ChatServer.cpp` 使用 `grpc::ServerBuilder` 注册 `ChatServiceImpl`，并在
独立线程中运行 `grpc_server->Wait()`；退出时关闭 gRPC server，再等待线程结束。

跨服客户端从各自 `config.ini` 的 `[PeerServer]` 读取对端配置。每个对端服务器会创建
一个大小为 5 的 `ChatService::Stub` 连接池，映射关系是：

```text
PeerServer 中的 name -> ChatConPool(host, rpcport)
```

这里必须连接对端的 `rpcport`，不能误填客户端 TCP 端口。取到 stub 后，调用代码会用
`Defer` 将其归还连接池；文本消息与踢人调用在连接池缺失或取到空 stub 时返回 `RPCFaild`。

## 4. 当前跨服文本消息链路

假设 A 在线于 `ChatServer1`，B 在线于 `ChatServer2`：

```text
客户端 A
  │ TCP：ID_TEXT_CHAT_MSG_REQ（fromuid、touid、textArray）
  ▼
ChatServer1
  │ Redis：uip_<B> -> ChatServer2
  │ gRPC：NotifyTextChatMsg(TextChatMsgReq)
  ▼
ChatServer2
  │ UserMgr：touid -> CSession
  │ TCP：ID_NOTIFY_TEXT_CHAT_MSG_REQ（fromuid、touid、textArray）
  ▼
客户端 B

ChatServer1
  │ TCP：ID_TEXT_CHAT_MSG_RSP（沿用 A 的 textArray 和最终 error）
  ▼
客户端 A
```

目标用户在同一台 ChatServer 时，服务端直接从本机 `UserMgr` 取 `CSession`，跳过 gRPC。
目标用户不在线、Redis 路由缺失，或路由指向的服务器上找不到会话时，当前实现会给发送方
返回 `UidInvalid`；gRPC 网络失败或没有相应连接池时，文本消息会返回 `RPCFaild`。

文本消息没有入库，也没有离线补投。发送方收到 `ID_TEXT_CHAT_MSG_RSP` 的成功码，表示
目标服务器已接受并向该 TCP 会话调用了 `Send`，不构成客户端 B 已展示消息的端到端确认。

## 5. 当前好友申请与认证链路

好友申请会先写入 MySQL 的 `friend_apply`，随后只在目标用户在线时尝试实时通知：

```text
客户端 A
  │ TCP：ID_ADD_FRIEND_REQ
  ▼
ChatServer1
  │ MySQL：写入 friend_apply
  │ Redis 路由到 ChatServer2 时，gRPC：NotifyAddFriend
  ▼
ChatServer2
  │ 找到 B 的 CSession 后，TCP：ID_NOTIFY_ADD_FRIEND_REQ
  ▼
客户端 B
```

B 不在线时，申请记录仍保留在数据库。B 下次登录时，登录回包会通过
`GetFriendApplyInfo` 带回待处理的 `apply_list`，所以实时通知缺失不会丢失申请。

当前客户端的认证请求默认表示“同意”：`AuthFriend` 在一个数据库事务中建立双方的
`friend` 记录并把申请状态更新为已同意。成功后，如果 A 在另一台服务器，B 所在的
ChatServer 调用 `NotifyAuthFriend`；对端查询 A 的会话和 B 的资料，再向 A 推送
`ID_NOTIFY_AUTH_FRIEND_REQ`。A 与 B 同服时直接推送，不走 gRPC。

> 当前业务没有“拒绝申请”的 TCP 处理分支。虽然 `RplyFriendReq.agree` 为未来的同意/拒绝
> 转发预留了字段，但当前 `ChatService` 已不声明 `RplyAddFriend` RPC，不能据此认为拒绝流程已经支持。

## 6. 业务 RPC 字段

### `NotifyAddFriend`

| 消息 | 字段 | 类型 | 含义 |
| --- | --- | --- | --- |
| 请求 `AddFriendReq` | `applyuid` | `int32` | 申请用户 ID |
|  | `name` | `string` | 申请用户账号名，用于展示 |
|  | `desc` | `string` | 申请附言 |
|  | `icon` | `string` | 申请用户头像 |
|  | `nick` | `string` | 申请用户昵称 |
|  | `sex` | `int32` | 申请用户性别 |
|  | `touid` | `int32` | 被申请用户 ID |
| 响应 `AddFriendRsp` | `error` | `int32` | 业务结果，`0` 表示成功 |
|  | `applyuid` | `int32` | 原样回传的申请用户 ID |
|  | `name` | `string` | proto 预留字段，当前服务端未填写 |
|  | `touid` | `int32` | 原样回传的目标用户 ID |

接收方服务端把申请内容转为 TCP JSON，字段为 `error`、`applyuid`、`name`、`desc`、
`icon`、`nick`、`sex`，然后发送 `ID_NOTIFY_ADD_FRIEND_REQ`。找不到目标会话时，该方法
当前仍返回 `Success`，因为申请已经由源服务器持久化，接收方登录时会从数据库加载。

### `NotifyAuthFriend`

| 消息 | 字段 | 类型 | 含义 |
| --- | --- | --- | --- |
| 请求 `AuthFriendReq` | `uid` | `int32` | 已处理申请的用户 ID，即认证者 |
|  | `touid` | `int32` | 原申请用户 ID，即通知目标 |
| 响应 `AuthFriendRsp` | `error` | `int32` | 业务结果，`0` 表示成功 |
|  | `uid` | `int32` | 原样回传的认证者 ID |
|  | `touid` | `int32` | 原样回传的原申请用户 ID |

接收方会用 `uid` 查询认证者资料，并发送 `ID_NOTIFY_AUTH_FRIEND_REQ`。TCP JSON 除
`error`、`uid`、`touid` 外还携带 `name`、`nick`、`desc`、`sex`、`icon`、`bakname`，让
原申请方客户端立即创建好友条目。如果认证者资料查询失败，响应为 `UidInvalid`；若原
申请方已经不在线，当前实现返回 `Success`，但不会产生实时通知。

### `NotifyTextChatMsg`

| 消息 | 字段 | 类型 | 含义 |
| --- | --- | --- | --- |
| 请求 `TextChatMsgReq` | `fromuid` | `int32` | 发送者 ID |
|  | `touid` | `int32` | 接收者 ID |
|  | `textmsgs` | `repeated TextChatData` | 要转发的消息数组 |
| `TextChatData` | `msgid` | `string` | 客户端 UUID，用于发送方关联 |
|  | `msgcontent` | `string` | 文本内容 |
| 响应 `TextChatMsgRsp` | `error` | `int32` | 业务结果，`0` 表示成功 |
|  | `fromuid`、`touid`、`textmsgs` | 对应字段 | proto 已定义，当前服务端未回填 |

当前跨服 proto 仍只传递 UUID 和正文。接收方将每项 `TextChatData` 转为 TCP JSON 的
`textArray`：`msgid` 保持不变，`msgcontent` 映射为 `content`；若目标 `CSession` 不存在，
服务端返回 `UidInvalid`，否则发送 `ID_NOTIFY_TEXT_CHAT_MSG_REQ`。实现按请求遍历顺序组装
数组，因而保留输入消息顺序。ChatServer1 的同服投递不经过该 proto：它在 MySQL 持久化后
额外携带 `message_id`、`thread_id`、收发者、状态和创建时间，客户端可实时写 SQLite。

### `NotifyKickUser`

| 消息 | 字段 | 类型 | 含义 |
| --- | --- | --- | --- |
| 请求 `KickUserReq` | `uid` | `int32` | 要在旧服注销的用户 ID |
|  | `session_id` | `string` | Redis 中记录的旧会话 ID，用于拒绝陈旧踢人请求 |
| 响应 `KickUserRsp` | `error` | `int32` | `Success`、`ServerBusy` 或 `RPCFaild` |
|  | `uid` | `int32` | 原样回传的用户 ID |

接收方从本机 `UserMgr` 查旧 session。会话已不存在时按幂等成功返回；存在时必须先与请求的
`session_id` 比较，相同才发送 `ID_NOTIFY_OFF_LINE_REQ`、注销并关闭旧 socket，不同则返回
`ServerBusy`。调用方设置 3 秒 deadline，只有收到成功响应才继续写入新登录路由。

## 7. 错误与兼容约定

gRPC `Status` 与 response 中的 `error` 是两层不同的结果：

| 情形 | 当前文本消息调用的结果 |
| --- | --- |
| 调用到达对端且已找到会话 | `Status::OK`，`error = Success` |
| 调用到达对端但会话不存在 | `Status::OK`，`error = UidInvalid` |
| 对端不可达、RPC 未实现或调用失败 | 非 OK `Status`，客户端封装转换为 `RPCFaild` |
| 目标不在线、Redis 没有路由 | 不发 gRPC，TCP 回包为 `UidInvalid` |

好友申请和好友认证的调用点目前没有根据 RPC response 决定其本地 TCP 回包；申请和认证的
成功分别以本地数据库写入、好友关系事务成功为准。因此接入新的可靠投递或补偿机制时，应
同时补齐这两处的 RPC 失败处理。

修改 `message.proto` 时必须保留已发布字段的编号与类型。新增字段使用新的编号；不要
复用或修改旧编号。两个 ChatServer、StatusServer、GateServer 与 Node.js 调用方都应
从同一份 proto 生成或加载协议代码，避免序列化不兼容。

## 8. 当前边界与后续实现方向

当前 ChatService 负责在线用户的服务间通知；ChatServer1 的 TCP 文本入口已先将消息持久化，
离线接收方可在下一次登录经 `ID_LOAD_CHAT_MSG_REQ/RSP` 增量拉取：

- gRPC `TextChatData` 尚未携带服务端 `message_id/thread_id`。因此跨服实时
  `ID_NOTIFY_TEXT_CHAT_MSG_REQ` 仍是兼容格式，目标客户端会在下一次
  `ID_LOAD_CHAT_MSG_REQ/RSP` 同步时补齐；两台服务升级 proto 后可携带这些字段并实时落库。
- 好友申请可离线保存，并在登录回包中加载；认证通知本身没有离线补投。
- `SendChatMsg` 是 proto 遗留接口。若要启用它，需要同时补齐
  `ChatServiceImpl`、`ChatGrpcClient`、调用方路由、业务错误处理及客户端 TCP 映射。
- 踢人 RPC 已设置 3 秒 deadline；好友和文本转发仍依赖 gRPC 默认等待行为。投入生产前应统一
  deadline，并按业务语义定义重试与幂等策略。
