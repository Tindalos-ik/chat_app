# 聊天信息存储方案



## 当前结构

目标是让客户端登录后先读取本地缓存，再按服务端消息 ID 增量同步离线期间的新会话和消息。



### 本地缓存

`LocalChatStorageMgr` 在 TCP 登录成功后按 UID 打开独立 SQLite 数据库，提供会话摘要、消息分页、同步游标和事务写入接口。



### 私聊会话

好友认证成功时 ChatServer1 会创建或复用唯一的 `chat_thread/private_chat`，并把首条认证消息写入 `chat_message`。好友资料页也可通过 `ID_CREATE_PRIVATE_CHAT_REQ/ID_CREATE_PRIVATE_CHAT_RSP` 显式取得同一个 `thread_id`；Qt 客户端会校验回包账号、写入 SQLite，并绑定对应 `ChatUserWid`。



### 同步边界

ChatServer1 收到 `ID_TEXT_CHAT_MSG_REQ` 后先创建/复用私聊，再将消息写入 `chat_message`。同服 `ID_TEXT_CHAT_MSG_RSP` / `ID_NOTIFY_TEXT_CHAT_MSG_REQ` 的每个 `textArray` 元素都携带 `message_id`、`thread_id`、发送/接收者、`created_at_ms` 和 `status`；桌面端收到带这些字段的 `ID_NOTIFY_TEXT_CHAT_MSG_REQ` 会在 SQLite 事务中保存消息、更新摘要和未读数。旧版通知缺少服务端 ID 时仍走原有内存展示路径，避免将不可去重的消息写入本地表。

登录成功后客户端先用 `ID_LOAD_CHAT_THREAD_REQ/RSP` 按 `after_thread_id` 发现新私聊，再用 `ID_LOAD_CHAT_MSG_REQ/RSP` 按每会话 `after_message_id` 正序分页加载历史。服务端会话表由 [sql/chat_message_storage.sql](../sql/chat_message_storage.sql) 创建，需在基础建表脚本之后手动执行。



## 数据模型设计

* 消息唯一标识
  * 在服务器端的mysql表中，为每条消息分配一个全局唯一的自增主键`message_id`，再配合时间戳`created_at`
  * 客户端本地用同样的`message_id`做主键，这样方便做增量同步与去重
* 会话/用户维度的索引
  * 如果支持群聊，再维护一个会话表`thread_id`和用户-会话关联表
  * 查询和分页时，按照`(thread_id, message_id)`建复合索引，加速筛选



## 同步流程

1.客户端登录

​	a.从本地SQLite加载最近N条信息，按`message_id`或时间倒序，渲染到界面

> 为什么本地选择SQLite？和MySQL区别？
>
> MySQL是服务端数据库，SQLite是嵌入式数据库
>
> SQLite直接作为一个库运行在客户端进程中，数据就是一个本地文件，不需要启动数据库服务，监听断开，管理账号或维护连接
>
> SQLite原生支持事务，索引，分页和全文检索

​	b.读取本地记录的 [每个会话已同步到的最大`message_id`] ，发送给服务器

```
{
  "action": "fetch_messages",
  "thread_id": 123, 
  "since_id": 3456
}
```

一个`thread_id`对应一个聊天窗口

2.服务器端响应

* 查询 在 `thread_id=123` 这个会话里，取 `message_id` 大于 3456 的最早 1000 条消息。

  ```
  SELECT * FROM messages
  WHERE thread_id = 123 AND message_id > 3456
  ORDER BY message_id ASC
  LIMIT 1000;
  ```

  优化，使用联合索引

  ```
  CREATE INDEX idx_thread_message ON messages (thread_id, message_id);
  ```

  `thread_id`在前，先按会话过滤，缩小范围

  `message_id`在后，在同一个`thread_id`，内，数据已经按照`message_id`有序排列

* 返回消息列表(可以分页返回，大量时前端可循环拉取，或返回`has_more`标记)

3.客户端接受并保存

* 将服务器返回的消息批量插入本地SQLite
* 更新本地 [已同步最大`message_id`]

4.后续聊天

* 新消息既推到服务器，也实时写入本地SQLite
* 如果走长连接，服务器收到新消息直接广播给在线客户端，服务器收到新消息后直接广播给在线客户端，并提示客户端写到本地
* 如果客户端离线，新消息积累在服务器，下一次登录再按流程拉取



## 聊天消息表

```
CREATE TABLE `chat_message` (
  `message_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `thread_id`  BIGINT UNSIGNED NOT NULL,
  `sender_id`  BIGINT UNSIGNED NOT NULL,
  `recv_id`    BIGINT UNSIGNED NOT NULL,
  `content`    TEXT        NOT NULL,
  `created_at` TIMESTAMP   NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` TIMESTAMP   NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `status`     TINYINT     NOT NULL DEFAULT 0 COMMENT '0=未读 1=已读 2=撤回',
  PRIMARY KEY (`message_id`),
  KEY `idx_thread_created` (`thread_id`, `created_at`),
  KEY `idx_thread_message` (`thread_id`, `message_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- `message_id`：全局自增主键，唯一标识一条消息。
- `thread_id`：会话（单聊、群聊）ID，同一会话下的所有消息共用一个 `thread_id`。
- `sender_id`：发送者用户 ID，指向用户表的主键。
- `recv_id` : 接收者用户ID，指向用户表主键
- `content`：消息正文，TEXT 类型，适合存储普通文字。
- `created_at`：消息创建时间，自动记录插入时刻。
- `updated_at`：消息更新时间，可用于标记“撤回”（status 变更）、编辑等操作。
- `status`：消息状态，用于标记未读/已读/撤回等（也可扩展更多状态）。



### 索引设计

* 主键索引：`PRIMARY KEY (message_id)`用于唯一检索消息
* 会话+时间索引：`key (thread_id, created_at)`支持按会话分页，按时间范围查询
* 会话+消息ID索引：`key (thread_id, message_id)`支持按`message_id`做增量拉取(`where thread_id =  ... and message_id > since_id)`



### 扩展

* 群聊用户表：如果支持群聊，需要一个`thread_memebr`表，记录每个`thread_id`下的成员及其角色
* 附件支持：如果要存储图片/文件，可额外建`message_attachment`表，字段例如`attachment_id`，`message_id`，`file_url`，`file_type`



## 会话消息表

> 聊天消息表中一条记录代表一条具体信息，归属于某个会话
>
> 会话消息表中，一条记录代表一个聊天会话

### 全局聊天会话表

建立`chat_thread`主表，给它一个全局自增`id`，记录所有私聊/群聊的`会话`统一入口

这个id就是别的地方的`thread_id`

```
CREATE TABLE chat_thread (
  `id`          BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `type`        ENUM('private','group') NOT NULL,
  `created_at`  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id)
);
```



### 单聊表设计

对于单聊，只有两个人，所以可以直接在`private_chat`表中定义两个字段存储`user1_id`和`user2_id`

```
CREATE TABLE `private_chat` (
  `thread_id`   BIGINT UNSIGNED NOT NULL COMMENT '引用chat_thread.id',
  `user1_id`    BIGINT UNSIGNED NOT NULL,
  `user2_id`    BIGINT UNSIGNED NOT NULL,
  `created_at`  TIMESTAMP     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`thread_id`),
  UNIQUE KEY `uniq_private_thread` (`user1_id`, `user2_id`), -- 保证每对用户只能有一个私聊会话
  -- 以下两行就是我们要额外加的复合索引
  KEY `idx_private_user1_thread` (`user1_id`, `thread_id`),
  KEY `idx_private_user2_thread` (`user2_id`, `thread_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

创建私聊时，服务端先把两个 uid 排序为 `(min(uidA, uidB), max(uidA, uidB))`，再按该顺序查询和写入。`uniq_private_thread` 是并发场景下的最终保障：两个请求同时发现私聊不存在时，只有一个能插入成功；另一个通过 `ON DUPLICATE KEY UPDATE thread_id = LAST_INSERT_ID(thread_id)` 获取已存在的会话 ID，并删除自己临时创建但未被引用的 `chat_thread`。因此调用方始终得到唯一的 `thread_id`，不会把“另一请求已创建成功”误判为失败。



### 群聊表设计

相对于单聊表，还需要记录每个群聊的多名成员及其角色，权限等信息

* 群聊会话表，存储群聊本身的信息，群名称，常见时间等

```
CREATE TABLE `group_chat` (
  `thread_id`   BIGINT UNSIGNED NOT NULL COMMENT '引用chat_thread.id',
  `name`        VARCHAR(255)  DEFAULT NULL COMMENT '群聊名称',
  `created_at`  TIMESTAMP     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`thread_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

群聊成员表，存储群聊中各个成员的信息，包括角色，加入时间，禁言等

```
CREATE TABLE `group_chat_member` (
  `thread_id`  BIGINT UNSIGNED NOT NULL COMMENT '引用 group_chat_thread.thread_id',
  `user_id`    BIGINT UNSIGNED NOT NULL COMMENT '引用 user.user_id',
  `role`       TINYINT       NOT NULL DEFAULT 0 COMMENT '0=普通成员,1=管理员,2=创建者',
  `joined_at`  TIMESTAMP     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `muted_until` TIMESTAMP    NULL COMMENT '如果被禁言，可存到什么时候',
  PRIMARY KEY (`thread_id`, `user_id`),
  KEY `idx_user_threads` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```



## 客户端同步流程

客户端本地会有`sql`记录该用户所有聊天记录最后收到的消息信息，包括`message_id`, `thread_id`等，每次客户端登录都会将本地最大`message_id`和`thread_id`发送给服务器，服务器按照每个`thread_id`将消息恢复给客户端，可支持分页返回



* 情况1，本地有`thread_id`，但是在该用户A离线的时候B用户给他发消息，因为他们之前没有聊过天，所以此时B就会通知服务器在`private_chat`表中创建新的`thread_id`，但是A本地数据库没有这个`thread_id`，所以A需要在登录时拉取

  拉取只需要传递目前A本地数据库中最大的`thread_id`以及`uid`给服务器，服务器去查找比这个`thread_id`大的会话列表返回即可，采取分页的方式，每次加载100个，并配合`load_more`字段通知客户端是否继续拉取

* 情况2，客户端换了新机器，本地没有记录信息，那么用户登录后就需要向服务器发送`uid`和`thread_id`，`thread_id`从0开始，服务器就需要返回该用户的所有聊天`thread_id`，必须分页返回，并且携带`load_more`字段

服务器返回数据格式

```
{
  "error":0,
  "uid" : 1001,
  "load_more":true,

  "threads":[
    {
      "thread_id": 1001,
      "type": "private",
      "user1_id": 1019,
      "user2_id": 1020
    },
    {
      "thread_id": 1002,
      "type": "group",
      "user1_id": 0,
      "user2_id": 0,
    },
    {
      "thread_id": 1003,
      "type": "private",
      "user1_id": 1019,
      "user2_id": 1021
    },
    {
      "thread_id": 1004,
      "type": "group",
      "user1_id": 0,
      "user2_id": 0
    }
  ] 
}
```



#### SQLite

Qt很简单就可以起来SQLite

1.CMake链接 Qt SQL模块

```
  find_package(Qt6 REQUIRED COMPONENTS Widgets Network Sql)

  target_link_libraries(chat_app_desktop PRIVATE
      Qt6::Widgets
      Qt6::Network
      Qt6::Sql
  )
```

2.按照`uid`打开独立数据库，例如`uid = 1001`，文件可放在 应用数据目录/chat_cache_1001.db

​	相关核心代码

```
  bool openLocalChatDb(qint64 uid)
  {
      const QString connectionName = QString("chat_cache_%1").arg(uid);

      QSqlDatabase db = QSqlDatabase::addDatabase("QSQLITE", connectionName);

      const QString dataDir =
          QStandardPaths::writableLocation(QStandardPaths::AppLocalDataLocation);
      QDir().mkpath(dataDir);

      db.setDatabaseName(
          dataDir + QString("/chat_cache_%1.db").arg(uid));

      if (!db.open()) {
          qDebug() << "SQLite 打开失败：" << db.lastError().text();
          return false;
      }

      QSqlQuery query(db);
      query.exec("PRAGMA foreign_keys = ON");
      query.exec("PRAGMA journal_mode = WAL");

      return true;
  }
```

3.首次打开由 `LocalChatStorageMgr::CreateSchema()` 执行幂等建表语句

4.收到服务端消息后，开启事务，使用预处理语句写入

```
  db.transaction();

  QSqlQuery query(db);
  query.prepare(R"(
      INSERT OR IGNORE INTO local_chat_message
      (message_id, thread_id, sender_id, recv_id, content, created_at_ms, updated_at_ms)
      VALUES (?, ?, ?, ?, ?, ?, ?)
  )");

  query.addBindValue(messageId);
  query.addBindValue(threadId);
  query.addBindValue(senderId);
  query.addBindValue(recvId);
  query.addBindValue(content);
  query.addBindValue(createdAtMs);
  query.addBindValue(createdAtMs);

  if (!query.exec()) {
      db.rollback();
      return false;
  }

  db.commit();
```

5.退出登录时关闭连接，并在所有QSqlQuery对象销毁后调用`QSqlDatebase::removeDatabase(connectionName)`



## 客户端实现

桌面客户端通过 `LocalChatStorageMgr` 管理 SQLite 缓存。ChatServer 登录成功、解析到 `uid` 后会调用 `Initialize(uid)`：

- 每个账号使用独立的 `chat_cache_<uid>.db`，文件位于 Qt 的 `AppLocalDataLocation/chat_cache/`；切换账号不会读取到另一个账号的缓存。
- 数据库文件不存在时，SQLite 自动创建，管理器随后执行幂等的 `CREATE TABLE IF NOT EXISTS` 建表；已存在时保留原有消息并加载会话摘要。
- 初始化只加载聊天列表需要的会话摘要，具体消息在用户打开会话时调用 `LoadRecentMessages()` 或 `LoadMessagesBefore()` 分页读取。这样不会因为本地历史很多而阻塞登录界面。
- `SaveReceivedMessages()` 在同一事务中更新会话、批量保存消息和每会话的最大同步游标；重复的 `message_id` 会更新原记录，因此撤回、已读等状态可被后续同步覆盖。

> ChatServer1 的 `ID_TEXT_CHAT_MSG_REQ` 已先创建/复用私聊，再写入 `chat_message`，并在 `ID_TEXT_CHAT_MSG_RSP` / `ID_NOTIFY_TEXT_CHAT_MSG_REQ` 的 `textArray` 项中返回 `message_id`、`thread_id`、发送/接收者、状态和本地展示时间。接收方离线时消息仍保留在 MySQL，发送回包以 `delivered:false` 表示未实时推送。`ID_LOAD_CHAT_THREAD_REQ/RSP` 以 `after_thread_id` 发现新增私聊，`ID_LOAD_CHAT_MSG_REQ/RSP` 以 `(thread_id, after_message_id)` 正序分页读取消息；两者均多取一条来返回 `load_more`。

客户端在 ChatServer 登录成功并打开 SQLite 后，立即以 `MaxKnownThreadId()` 发送 `ID_LOAD_CHAT_THREAD_REQ`。每页 `ID_LOAD_CHAT_THREAD_RSP` 将私聊元数据写入本地；若 `load_more=true`，继续以 `next_thread_id` 请求下一页。写入时保留已有的消息摘要、时间和未读数，并向聊天列表绑定正式 `thread_id`。

随后客户端会遍历本地全部私聊和 `SyncCursors()`，为每个会话发送 `ID_LOAD_CHAT_MSG_REQ(thread_id, after_message_id)`；`ID_LOAD_CHAT_THREAD_RSP` 新发现的会话也立即进入同一流程。`ID_LOAD_CHAT_MSG_RSP` 中的消息和更新后的游标通过 `SaveReceivedMessages()` 同一 SQLite 事务提交，分页未结束时继续使用 `next_message_id` 请求下一页。

用户点击聊天列表或从联系人页进入聊天时，`ChatDialog` 优先根据 `peerUid` 找到正式 `thread_id`，清除上一会话的气泡，再调用 `LoadRecentMessages(thread_id)`。消息按 `message_id` 正序渲染，`sender_id` 决定左右气泡；会话打开后调用 `MarkThreadRead()` 清除本地未读数。这样历史记录始终来自 SQLite，不依赖在线推送还留在内存中。

首次打开只读取最近 50 条。用户滚到聊天窗口顶部时，`ChatView` 发出顶部信号，
`ChatDialog` 以当前最小 `message_id` 调用 `LoadMessagesBefore()` 再取 50 条；结果仍按
正序头插，并按滚动条高度增量补偿位置，因此屏幕不会因插入更早消息跳到别处。少于一页或
查询为空时停止继续读取；整个过程只查询本地 SQLite，不额外向 ChatServer 发请求。

每个 `ID_LOAD_CHAT_MSG_RSP` 页在 SQLite 事务提交后会发送本地同步完成信号。聊天列表据最新缓存摘要刷新最后消息、时间、未读红点；若该会话正在打开，则重新读取当前本地历史页并渲染，保证增量消息即时可见。

同服实时 `ID_NOTIFY_TEXT_CHAT_MSG_REQ` 使用和 `ID_LOAD_CHAT_MSG_RSP` 相同的 `SaveReceivedMessages()` 写入边界：先利用
`message_id` 与每会话同步游标判断是否为重复通知，再在同一事务中写消息、更新会话摘要、
更新游标。未打开会话的好友消息才增加未读数；正在查看该会话时直接写为已读。事务提交后
聊天列表从 SQLite 刷新，因此实时通知早于会话列表回包到达时，也能补建带正式 `thread_id`
的列表项。跨 ChatServer 的 protobuf 当前只转发 UUID 和正文，目标端收到的是旧格式通知；
该场景需在两台 ChatServer 同步扩展 proto 字段，或依靠下一次登录的 `ID_LOAD_CHAT_MSG_REQ/RSP` 增量加载补齐。

发送方收到成功 `ID_TEXT_CHAT_MSG_RSP` 时，也会按 `thread_id` 聚合本批确认消息并写入 SQLite。这样界面中
发送前的临时气泡会被服务端确认的 `message_id` 记录替换，客户端重启前后都能保留自己发送的
内容；失败回包没有可靠的服务端主键，暂不写入该表，发送失败态仍由发送 UI 管理。

发送气泡所在的 `ChatItemBase` 在气泡外侧预留状态 `QLabel`：自己的气泡左端、对方的气泡
右端各有一个镜像位置，默认隐藏。服务端回包失败时，客户端根据原始 `msgid` 找回乐观展示的
消息行，在自己的状态标签显示 `:/res/send_fail.png`；服务端会在失败回包中保留原始 `textArray`
的 `msgid`，避免客户端无法定位失败气泡。成功回包不显示图标，并由确认后的 SQLite 消息重绘。

创建或更新认证产生的 `ChatUserWid` 时，客户端始终显示该会话 `lastMessageId` 对应的
最新消息摘要和 `lastMessageAtMs` 的 `HH:mm` 时间；重复认证通知或较旧消息不能把这两个值覆盖回去。

对外主要接口：

| 接口                                                      | 用途                                |
| --------------------------------------------------------- | ----------------------------------- |
| `Initialize(uid)`                                         | 打开/创建该账号数据库并加载会话摘要 |
| `CachedThreads()`、`LoadThreads()`                        | 获取本地聊天列表                    |
| `LoadRecentMessages(threadId)`、`LoadMessagesBefore(...)` | 正序分页读取某会话的历史消息        |
| `UpsertThread()`                                          | 保存服务器下发的会话元数据          |
| `SaveReceivedMessages()`                                  | 事务保存一批同步消息及其游标        |
| `SyncCursors()`、`MaxKnownThreadId()`                     | 组装登录后的增量同步请求            |
| `MarkThreadRead()`                                        | 打开会话后清除本地未读状态          |





## 首次单聊

A和B是好友，首次单聊，A发送给服务器创建聊天的请求，服务器根据A的创建请求创建私聊，然后返回给客户端A

在分布式的情况下，聊天服务是异步的情况下，有可能对方B就在此时发送消息给A，服务器已经创建好了，或者服务器正在调用`sql`创建

所以对于创建请求，sql需要先查询其他人是否已经创建了`thread_id`，对此，我们可以设定，在写入私聊表`private_chat`的时候，要保证`uid1_id < uid2_id`

这个查询需要加行级锁



1.查询是否已经存在私聊会话，如果存在则加锁行并返回`thread_id`

```
SELECT thread_id
FROM private_chat
WHERE (user1_id = LEAST(:user1_id, :user2_id) AND user2_id = GREATEST(:user1_id, :user2_id))
  FOR UPDATE;  -- 使用行级锁，避免并发冲突
```

`FOR UPDATE`关键字会锁定查询行，确保在事务结束之前不会有其他并发的操作修改数据



2.如果未找到数据，则插入新纪录

```
--    1. 在 chat_thread 表中创建新记录
INSERT INTO chat_thread (type, created_at)
VALUES ('private', NOW());
--    2. 获取新插入的 thread_id（假设你可以通过 LAST_INSERT_ID 获取）
SELECT LAST_INSERT_ID();
```

将新生成的`thread_id`插入`private_chat`表

```
INSERT INTO private_chat (thread_id, user1_id, user2_id, created_at)
VALUES (:new_thread_id, LEAST(:user1_id, :user2_id), GREATEST(:user1_id, :user2_id), NOW());
```



可能的并发问题，例如

* 线程A执行查询，锁定了`private_chat`表的行
* B也执行了相同的查询，发现没有记录，于是开始插入`chat_thread`
* A完成插入`chat_thread`和`private_chat`，但线程B也在此时完成了它的插入，导致`private_chat`表中出现两个重复的记录



解决方案，使用事务来保证数据的一致性和原子性，保证同一时间只有一个请求成功创建`chat_thread`和`private_chat`



### 客户端建立私聊请求

用户在好友资料页点击“发消息”时，客户端向当前已连接的 ChatServer 发送 `ID_CREATE_PRIVATE_CHAT_REQ`：

```json
{
  "uid": 1001,
  "other_id": 1002
}
```

`uid` 保留用于协议兼容和回包展示，服务端不能信任它，必须使用已认证 `CSession` 中的 uid；`other_id` 是用户选择的好友。服务端返回 `ID_CREATE_PRIVATE_CHAT_RSP`，其中携带 `error`、`uid`、`other_id` 和唯一的 `thread_id`。客户端后续应以这个 `thread_id` 创建或更新本地会话，而不是自行生成会话 ID。

桌面端收到 `ID_CREATE_PRIVATE_CHAT_RSP` 后会校验回包 uid 与当前登录用户一致，再将 `thread_id`、私聊类型和对方 uid 写入 `LocalChatStorageMgr` 的 `local_chat_thread` 表，并更新对应 `ChatUserWid`。列表项持有 `thread_id`，用户再次点击该项时可将它用于历史消息分页、发送消息和同步游标；重复的创建回包只更新同一项，且不会覆盖已有的最后消息和未读数。



### 客户端消息类

客户端使用 `ChatDataBase` 抽象消息共有字段：服务端 `messageId`、`threadId`、创建时间、会话类型、消息类型、内容、发送者、状态和客户端 UUID。`TextChatData` 继承该基类，另外保留旧的 `TextChatData(QString msgId, QString content, int fromUid, int toUid)` 构造函数以及 `_msg_id`、`_msg_content`、`_from_uid`、`_to_uid` 字段，因此旧版文本发送和在线推送仍可继续工作。带服务端 ID 的新构造函数用于实时持久化与增量同步。



