# 心跳检测



## 心跳概念

在一个长连接(如TCP持久连接，WebSocket，gRPC长连等)中，客户端和服务器会保持一个持续打开的通道，以便双方随时双向发送数据，比起一次性请求/短连接，长连接最大的挑战在于如何做到及时发现网络或对端异常，并防止连接在中间节点(如路由器，NAT，防火墙)被静默回收



原有的被动离线处理是在 TCP 读写报错后关闭 socket、清理 Session。现在心跳超时也会主动调用同一个清理入口。

它可以处理

* 用户正常关闭客户端
* 客户端主动断开TCP
* TCP收到 FIN/RST
* 读写时发现网络错误
* 服务端向已失效连接发送数据时报错

这个属于是被动方法，是由OS明确告诉程序连接坏了，清理才会发生



为什么还需要心跳？下面这些场景，TCP可能很长时间都不会报错

* 客户端突然断电

* 拔网线，或 Wi-Fi 突然消失

* NAT，防火墙静默删除空闲连接

  > 许多网络设备会对空闲连接设置一个超时阈值，如果某段时间内连接上没有任何数据包经过，它会自动回收这条路由/会话，导致真正的数据到达时被丢弃或重置
  >
  > 心跳包可以视作活动信号，让中间设备认为连接仍在活跃，从而维持映射表或会话状态，避免连接断开

* 长连接一直没有业务消息，失联后就缺少触发读写错误的机会。空闲不等于失联，只要心跳正常，就应保留连接。



## 工作流程

心跳采用客户端 Ping、服务端 Pong 的应用层协议，使用已有 TCP 包头，不新增连接：

| 消息 ID | 方向 | 包体 | 作用 |
| ---: | --- | --- | --- |
| `1023` `ID_HEART_BEAT_REQ` | Qt 客户端 → ChatServer | `{"client_time":"..."}` | 表示客户端仍存活 |
| `1024` `ID_HEARTBEAT_RSP` | ChatServer → Qt 客户端 | `{"error":0,"server_time":...}` | 确认服务端仍可处理连接 |

时间参数如下：

| 参数 | 值 | 说明 |
| --- | ---: | --- |
| 客户端发送间隔 | 20 秒 | TCP 登录成功后立即发送首包，随后周期发送 |
| 客户端无成功 1024 阈值 | 60 秒 | 在发送定时器触发时检查，超时后 `abort()`，复用断线提示流程 |
| 服务端无完整收包阈值 | 60 秒 | 达到阈值后按失联处理，半包不续期 |
| 服务端扫描间隔 | 10 秒 | 事件循环正常调度时，约在最后收包后 60～70 秒发现超时 |

客户端每 20 秒检查一次，所以通常在最后一个成功回包后约 60～80 秒发现超时。上述是正常调度下的估计，
不是硬实时保证；线程阻塞、系统休眠、发送积压都可能延迟检测。超时表示连接在允许时间内不可用，
不能区分断电、网络分区和暂时拥塞。

`client_time` 是客户端 Unix 毫秒时间的字符串，`server_time` 是服务端 Unix 秒时间；仅用于诊断，
不互相相减，也不参与存活判断。服务端接受 JSON 对象形式的 1023（可用 `{}`），畸形 JSON 或非对象会断开。



### 客户端

`TcpMgr` 在收到 TCP 登录成功回包后启动 `QTimer`，并立即发送第一条 1023。每次定时器触发时先检查距离上一个有效
`1024` 是否已超过 60 秒；若超时，即使 `QTcpSocket` 仍显示 `ConnectedState`，也主动关闭
连接，并弹出“心跳超时”提示后返回登录页。心跳超时使用独立信号，不再触发普通连接断开弹窗，
避免一次超时连续显示两个提示。否则发送 `1023`。收到格式正确且 `error == 0` 的 `1024` 后，使用 `QElapsedTimer`
刷新最后响应时间。

客户端断开、收到 1021、主动退出或准备连接新 ChatServer 时都会停止心跳定时器，避免旧连接的定时器
继续发送数据。



### 服务端

每个 `CSession` 保存 `steady_clock` 的原子毫秒计数，从 accept 成功后 `Start()` 开始计时，
避免把等待客户端接入的时间算成连接空闲。单调时钟不受系统时间前拨、回拨影响。

完整业务帧和 JSON 对象形式的心跳会刷新活跃时间；半包不刷新。业务帧这里只校验包头和长度，
其 JSON、消息 ID 和权限仍交给业务层校验，因此活跃并不代表业务合法或登录成功。
服务端允许未登录连接发送 1023，仅返回传输层 Pong，不授予业务权限；无数据的未登录连接也会超时清理。

对于 `1023`，`CSession` 直接将 `1024` 放进发送队列，不经过单线程业务队列。
`LogicSystem::DealMsg()` 也改为取出消息后释放队列锁再执行业务，避免数据库/RPC 等待挡住 I/O 线程入队和后续收包。
Pong 检测的是收发路径可用性，不代表数据库或所有业务线程健康。

两台 ChatServer 的 `CServer::steady_timer` 每 10 秒扫描 `_sessions`。扫描时只在锁内收集
超时的 `shared_ptr<CSession>`，释放 `_mutex` 后将检查任务 post 到会话自己的 I/O executor。
执行时再次检查最后活跃时间，仍超时才调用 `HandleDisconnect()`，避免扫描后刚收到心跳却仍被关闭。
不能持有 `_mutex` 同步清理，否则内部的 `ClearSession()` 会再次申请同一锁，造成自锁死锁。

`HandleDisconnect()` 统一关闭 socket、注销本机 Session/UserMgr、修正在线数，并仅在 Redis
中的 `session_id` 仍等于当前连接时删除 `usession_<uid>` 与 `uip_<uid>`。
主监听/扫描线程不再同步等待 Redis；会话 I/O 线程中的清理仍使用同步 Redis 操作，
依赖故障时仍可能阻塞该线程，后续应独立处理远端清理并补充超时和重试。

> 心跳能发现客户端失联和 ChatServer 对客户端不可用，但客户端尚未实现自动重连、重新
> 获取路由和重新登录；ChatServer 进程崩溃留下的 Redis 路由仍需 TTL/租约解决。



## 验证方式与当前边界

本次只做源码静态核对，按要求未编译、未运行端到端测试。下面是待运行的复测步骤：

| 场景 | 操作 | 预期 |
| --- | --- | --- |
| 正常空闲 | 分别连接两台 ChatServer，登录后不聊天至少 3 分钟 | 有 1023/1024 往返，不因空闲被踢 |
| 服务端超时 | 测试 TCP 连接不发数据，或发送半包后停止 | 正常调度下约 60～70 秒被关闭；未登录连接不操作 uid=0 路由 |
| 客户端超时 | 测试环境中静默丢弃服务端回包，避免 FIN/RST | 最后成功 Pong 后约 60～80 秒只弹一次“心跳超时”，随后返回登录页 |
| 重复登录 | 同服、跨服分别登录同一账号 | 旧端 1021 后停止心跳，退出只提示一次，新端持续心跳 |
| 协议异常 | 发送 1023，包体为非 JSON 或数组 | 按协议错误断开，不刷新心跳 |
| 半包/粘包 | 拆分一条 1023，或一次写入多条完整 1023 | 读满后才应答，每个完整心跳各回一条 1024 |
| 再次登录 | 超时后手动重新登录 | 新连接重新启动心跳，不沿用旧超时计时 |

客户端只按成功 1024 续期，普通聊天回包不替代 Pong。当前没有心跳序号关联、独立登录超时、
Redis 路由续租、离线消息补投；发送队列拥堵或 I/O 线程阻塞时也可能触发超时。
心跳不是消息送达 ACK，也不能单独保证在线状态在 Redis 故障时一致。



## 为什么断线清理要设计成幂等入口

幂等的含义是：同一个操作执行一次或重复执行多次，最终状态保持一致，不会因为重复执行
产生额外副作用。`CSession::HandleDisconnect()` 是所有断线场景共用的清理入口，因此必须
具备幂等性。

一次连接失效可能同时触发多条异步路径。例如，心跳线程先发现超时并调用
`HandleDisconnect()`，函数关闭 socket 后，正在等待的读回调和写回调又会收到错误，它们
也会调用同一个清理函数：

```text
心跳超时
  └─ HandleDisconnect()
       └─ Close()
            ├─ 读回调收到错误 ──► HandleDisconnect()
            └─ 写回调收到错误 ──► HandleDisconnect()
```

如果每次调用都完整执行清理，可能重复删除 Session、重复修改 Redis，尤其可能让
`login_count` 多次减一。因此会话中使用原子标记 `_disconnect_handled`：

```cpp
void CSession::HandleDisconnect() {
    if (_disconnect_handled.exchange(true)) {
        return;
    }

    // 只有第一次调用能够执行完整清理。
    Close();
    _server->ClearSession(_session_id);
    // 按当前 session_id 条件清理 Redis 在线路由……
}
```

`exchange(true)` 会以一个不可分割的原子操作完成两件事：把标记设置为 `true`，同时返回
修改前的值。第一次调用得到旧值 `false`，继续执行；后续调用得到旧值 `true`，立即返回。
即使多个 I/O 线程同时进入该函数，也只有一个线程能够执行完整清理。

这里的幂等保护分为三层：

1. `_disconnect_handled` 防止同一个 `CSession` 重复进入完整清理流程；
2. `CServer::ClearSession` 只在 `_sessions` 中确实找到该会话时才减少在线数；
3. Redis 清理前比较 `session_id`，避免旧连接的延迟回调删除同一用户的新登录路由。

> 幂等入口解决的是进程内的重复回调问题，并不等于 Redis 多条命令已经具备事务性。
> `usession_<uid>` 与 `uip_<uid>` 的条件删除后续仍应收敛为 Lua 原子操作。
