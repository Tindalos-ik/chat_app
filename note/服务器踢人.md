# 服务器踢人

本文按当前项目源码说明重复登录时的同服踢人、跨服踢人、1021 下线通知和连接清理。
ChatServer1 与 ChatServer2 的相关实现目前一致，下面以 ChatServer1 为例。

主要代码位置：

- `ChatServer1/src/LogicSystem.cpp`：登录时判断旧会话位置；
- `ChatServer1/src/ChatServiceImpl.cpp`：处理跨服踢人 RPC；
- `ChatServer1/src/ChatGrpcClient.cpp`：调用旧服；
- `ChatServer1/src/CSession.cpp`：发送 1021、断线清理；
- `ChatServer1/src/CServer.cpp`：注销 TCP session、维护在线数；
- `ChatServer1/src/UserMgr.cpp`：维护本机 `uid -> CSession`；
- `chat_app desktop/tcpmgr.cpp`：客户端解析 1021 和断线兜底；
- `proto/message.proto`：`NotifyKickUser` 协议。

## 1. 当前实现要保证什么

同一个 uid 再次登录时，系统保留新连接，让旧连接失效：

```text
客户端 A 已登录
    ↓
客户端 B 使用同一 uid 登录
    ↓
定位 A 所在的 ChatServer
    ↓
向 A 排队发送 1021
    ↓
从旧服会话表注销 A
    ↓
发送队列排空后关闭 A 的 socket
    ↓
登记 B 的新路由和新 session
```

旧连接和新连接拥有相同 uid，因此不能只按 uid 清理。当前实现同时比较 `session_id`，避免
旧连接晚到的异步回调删除新连接。

> 踢人时服务端会主动关闭旧 socket。`TIME_WAIT` 是 TCP 主动关闭方可能出现的正常状态，
> 不能为了回避它而保留已经失效的重复登录连接。

## 2. 踢人链路中的状态

| 位置 | 数据 | 含义 |
| --- | --- | --- |
| Redis | `uip_<uid>` | 用户当前所在的 ChatServer 名称 |
| Redis | `usession_<uid>` | 用户当前有效 session 的 UUID |
| Redis Hash | `login_count` | 每台 ChatServer 的在线会话数 |
| Redis | `lock:login_lock_<uid>` | 同一 uid 登录、断线清理的分布式锁 |
| `UserMgr` | `uid -> CSession` | 本机按 uid 查当前连接 |
| `CServer` | `session_id -> CSession` | TCP 会话表和生命周期入口 |

`uip_<uid>` 只负责跨服定位，真正的 socket 仍在目标 ChatServer 的内存中。

## 3. LogicSystem 和 gRPC 必须共享同一个 CServer

ChatServer 启动时只创建一次 TCP `CServer`，再把同一个实例交给登录逻辑和 gRPC 服务：

```cpp
// ChatServer.cpp
auto server = std::make_shared<CServer>(
    io_context,
    std::stoi(config["SelfChatServer"]["port"]));

LogicSystem::GetInstance()->SetServer(server);

ChatServiceImpl service;
builder.RegisterService(&service);
service.RegisterServer(server);
```

这样 `LoginHandler` 和 `NotifyKickUser` 操作的是同一份 `CServer::_sessions`。如果分别创建
两个 `CServer`，gRPC 即使收到请求，也找不到 TCP 服务实际接入的旧客户端。

## 4. 登录锁和处理顺序

token、用户资料和好友数据准备完成后，`LoginHandler` 获取分布式锁：

```cpp
auto lock_key = LOGIN_LOCK_PREFIX + std::to_string(uid);
auto lock_result = RedisMgr::GetInstance()->acquireLock(
    lock_key, LOCK_TIME_OUT, ACQUIRE_TIME_OUT);

if (lock_result.result != RedisLockResult::Acquired) {
    rtvalue["error"] = ErrorCode::ServerBusy;
    return;
}

Defer lock_defer([this, identifier, lock_key]() {
    RedisMgr::GetInstance()->releaseLock(lock_key, identifier);
});
```

当前配置：

| 项目 | 时间 |
| --- | ---: |
| 获取锁最多等待 | 5 秒 |
| 锁租约 | 10 秒 |
| 跨服踢人 RPC deadline | 3 秒 |

锁内顺序是：

```text
读取 uip_<uid>、usession_<uid>
  -> 踢旧会话
  -> 新服 login_count +1
  -> 新 session 绑定 uid
  -> 写新的 uip_<uid>、usession_<uid>
  -> 写入本机 UserMgr
  -> 解锁
```

跨服 RPC 在锁内同步执行。旧服失败时，新服不会继续覆盖在线路由。

## 5. 同服踢人

Redis 的 `uip_<uid>` 指向本服时，直接从 `UserMgr` 查旧 session：

```cpp
auto old_session = UserMgr::GetInstance()->GetSession(uid);
if (old_session) {
    if (old_session_id.empty()
        || old_session->GetSessionId() != old_session_id) {
        rtvalue["error"] = ErrorCode::ServerBusy;
        return;
    }

    if (!_p_server) {
        rtvalue["error"] = ErrorCode::ServerBusy;
        return;
    }

    old_session->NotifyOffline();
    _p_server->ClearSession(old_session->GetSessionId());
}
```

这里有两层保护：

1. Redis 中必须存在旧 `session_id`；
2. Redis 的 ID 必须等于本机旧 session 的 ID。

不匹配时返回 `ServerBusy(1014)`，不能按 uid 盲踢。如果 Redis 指向本服，但 `UserMgr`
已经找不到旧 session，当前代码会跳过踢人，继续登记新会话并覆盖旧路由。

同服踢人先让旧会话的 `login_count -1`，新会话登记时再 `+1`，正常情况下在线数净变化为
0。

## 6. 跨服踢人

### 6.1 RPC 协议

新服把 uid 和自己从 Redis 读到的旧 `session_id` 一起发给旧服：

```protobuf
message KickUserReq {
    int32 uid = 1;
    string session_id = 2;
}

message KickUserRsp {
    int32 error = 1;
    int32 uid = 2;
}

service ChatService {
    rpc NotifyKickUser (KickUserReq) returns (KickUserRsp) {}
}
```

`session_id` 是会话版本。它用于拒绝延迟到达的旧 RPC，避免旧请求误踢后来建立的新连接。

### 6.2 新服发起调用

```cpp
KickUserReq request;
request.set_uid(uid);
request.set_session_id(old_session_id);

auto response =
    ChatGrpcClient::GetInstance()->NotifyKickUser(uid_ip_value, request);

if (response.error() != ErrorCode::Success) {
    rtvalue["error"] = response.error();
    return;
}
```

`ChatGrpcClient` 默认把结果设为 `RPCFaild`，只有连接池存在、拿到 stub、gRPC 在 3 秒内
返回且业务结果成功，登录才继续：

```cpp
KickUserRsp rsp;
rsp.set_error(ErrorCode::RPCFaild);
rsp.set_uid(request.uid());

auto find_iter = _pools.find(server_ip);
if (find_iter == _pools.end()) {
    return rsp;
}

ClientContext context;
const auto deadline =
    std::chrono::system_clock::now() + std::chrono::seconds(3);
context.set_deadline(deadline);

auto stub = pool->getConnectionUntil(deadline);
if (!stub) {
    return rsp;
}

Status status = stub->NotifyKickUser(&context, request, &rsp);
if (!status.ok()) {
    rsp.set_error(ErrorCode::RPCFaild);
}
```

### 6.3 旧服接收调用

```cpp
auto session = UserMgr::GetInstance()->GetSession(uid);

if (session == nullptr) {
    // 目标已经不在本机，按幂等成功处理。
    response->set_error(ErrorCode::Success);
    return Status::OK;
}

if (request->session_id().empty()
    || session->GetSessionId() != request->session_id()) {
    response->set_error(ErrorCode::ServerBusy);
    return Status::OK;
}

if (!_p_server) {
    response->set_error(ErrorCode::RPCFaild);
    return Status::OK;
}

session->NotifyOffline();
_p_server->ClearSession(session->GetSessionId());
```

| 旧服状态 | 返回结果 |
| --- | --- |
| 已没有该 uid 的 session | `Success`，幂等成功 |
| session 存在且 ID 匹配 | 排队发送 1021、注销会话，返回 `Success` |
| ID 为空或不匹配 | `ServerBusy` |
| 没有注册真实 `CServer` | `RPCFaild` |

> RPC 返回 `Success` 只表示旧服已经排队发送 1021 并注销本机会话，不表示旧客户端已经解析
> 1021。当前没有客户端确认回执。

## 7. 1021 如何保证先发送再关闭

1021 是 `ID_NOTIFY_OFF_LINE_REQ`，消息体为：

```json
{
  "error": 0,
  "uid": 1001
}
```

`CSession::NotifyOffline()` 不会调用普通 `Send()` 后立即关 socket，而是在发送锁内追加控制
消息：

```cpp
std::lock_guard<std::mutex> lock(_send_lock);
if (_close_after_send) {
    return;
}

const bool write_in_progress = !_send_que.empty();
_send_que.push(std::make_shared<SendNode>(
    return_str.c_str(),
    static_cast<short>(return_str.length()),
    ID_NOTIFY_OFF_LINE_REQ));
_close_after_send = true;

if (!write_in_progress) {
    auto &msgnode = _send_que.front();
    boost::asio::async_write(
        _socket,
        boost::asio::buffer(msgnode->_data, msgnode->_total_len),
        std::bind(&CSession::HandleWrite,
                  this,
                  std::placeholders::_1,
                  SharedSelf()));
}
```

`_close_after_send` 设为 true 后，普通业务 `Send()` 不再入队。`HandleWrite()` 会继续发送队列
里的节点，队列排空后才关闭：

```cpp
_send_que.pop();
if (!_send_que.empty()) {
    // 继续 async_write 下一条
} else if (_close_after_send) {
    should_close = true;
}

if (should_close) {
    Close();
}
```

异步回调持有 `SharedSelf()`。因此旧 session 即使已经从 `CServer` 和 `UserMgr` 注销，对象
仍能活到发送完成。

## 8. ClearSession 与 Redis 清理

`CServer::ClearSession` 负责本机注销和在线数：

```cpp
auto iter = _sessions.find(session_id);
if (iter != _sessions.end()) {
    uid = iter->second->GetUserId();
    if (uid != 0) {
        UserMgr::GetInstance()->RmvUserSession(uid, session_id);
    }
    _sessions.erase(iter);
}

if (uid != 0) {
    RedisMgr::GetInstance()->HIncrBy(
        LOGIN_COUNT, server_name, -1);
}
```

`UserMgr::RmvUserSession(uid, session_id)` 也会核对 ID：

```cpp
if (iter->second->GetSessionId() != session_id) {
    return;
}
_uid_to_session.erase(iter);
```

所以旧连接的延迟回调不能移除同 uid 的新连接。`ClearSession` 只在 `_sessions` 中确实找到
连接时减少在线数，重复调用不会重复减。

`ClearSession` 本身不删除 `uip_<uid>` 和 `usession_<uid>`：

- 踢人时，旧路由由同一次新登录随后覆盖；
- 普通网络断线时，`CSession` 的读错误回调获取登录锁，比较 Redis session ID，匹配后才
  删除两个 key。

普通断线的关键代码为：

```cpp
std::string redis_session_id;
auto success = RedisMgr::GetInstance()->Get(
    USER_SESSION_PREFIX + uid_str,
    redis_session_id);
if (!success) {
    return;
}

if (redis_session_id != _session_id) {
    return;
}

RedisMgr::GetInstance()->Del(USER_SESSION_PREFIX + uid_str);
RedisMgr::GetInstance()->Del(USERIPPREFIX + uid_str);
_server->ClearSession(_session_id);
```

会话 ID 不匹配时不能删除 Redis，因为此时 Redis 很可能已经属于新登录。

## 9. 客户端收到 1021 后做什么

`TcpMgr` 注册了 1021 handler：

```cpp
_handler[ID_NOTIFY_OFF_LINE_REQ] =
    [this](ReqId, int, QByteArray data) {
        const QJsonDocument jsonDoc = QJsonDocument::fromJson(data);
        if (!jsonDoc.isObject()) {
            return;
        }

        const QJsonObject jsonObj = jsonDoc.object();
        if (jsonObj.value("error").toInt(ErrorCodes::ERR_JSON)
            != ErrorCodes::SUCCESS) {
            return;
        }

        if (_disconnect_notified) {
            return;
        }
        _disconnect_notified = true;
        _logged_in = false;
        emit sig_off_line();
    };
```

`MainWindow::SlotOffline` 弹出“同账号异地登录”，关闭客户端连接并切回登录页。

如果客户端没有完整解析到 1021，服务端随后关闭 socket，`QTcpSocket::disconnected` 会走
兜底：

```cpp
const bool should_notify = _logged_in && !_disconnect_notified;
_logged_in = false;
_disconnect_notified = true;

if (should_notify) {
    emit sig_connection_lost();
}
```

这条路径显示普通“连接断开”提示。它只能说明 socket 断了，不代表客户端收到了 1021。
`_disconnect_notified` 用于避免 1021 和断线事件重复弹窗。

## 10. 错误码与诊断日志

| 情况 | 结果 |
| --- | --- |
| 获取登录锁失败 | `ServerBusy(1014)` |
| 同服或跨服 session ID 不匹配 | `ServerBusy(1014)` |
| 连接池缺失、无 stub、gRPC 失败或超时 | `RPCFaild(1002)` |
| 旧服已没有目标 session | `Success(0)` |
| 踢人成功 | 新登录继续登记并返回 `Success(0)` |

跨服成功时应出现：

```text
cross-server kick request, uid = ..., old server = ..., session = ...
notify old client offline, uid = ..., session = ...
cross-server kick succeeded, uid = ...
```

异常日志：

| 日志 | 含义 |
| --- | --- |
| `kick target already offline` | 旧服已没有该会话，按幂等成功 |
| `reject stale kick request` | 请求的 session ID 与当前连接不一致 |
| `cross-server kick failed` | 踢人失败，新登录不覆盖旧路由 |

当前 `NotifyKickUser` 的 gRPC 失败分支没有打印 `Status` 详情。源码中
`NotifyKickUser RPC failed` 这段文字实际位于 `NotifyAddFriend` 的失败分支，是误写，不能
用于判断踢人 RPC 是否失败。

客户端对 TCP 登录失败统一显示“登录失败”，没有区分 `RPCFaild` 与 `ServerBusy`。

## 11. 复测方式

### 同服

1. 让 A 和 B 都连接同一台 ChatServer；
2. A 先登录，B 再登录同一账号；
3. A 应收到异地登录提示并返回登录页；
4. B 应登录成功；
5. `uip_<uid>`、`usession_<uid>` 应对应 B。

### 跨服

1. A 登录 ChatServer1；
2. B 使用同一账号登录 ChatServer2；
3. 检查三条跨服成功日志；
4. A 应优先收到 1021；即使通知未完整解析，也应因 socket 断开返回登录页；
5. B 登录成功后，Redis 路由应指向 ChatServer2。

### RPC 失败

让 Redis 仍指向旧服，但停止旧服 gRPC，再发起新登录。预期约 3 秒后登录失败，且旧路由
不能被新服覆盖。

## 12. 当前代码仍存在的问题

1. `uip_<uid>` 和 `usession_<uid>` 没有 TTL、心跳和自动剔除。ChatServer 崩溃会留下
   陈旧路由。
2. `login_count` 在正常退出时删除、启动时重置，但进程崩溃时仍可能短期失真。
3. 登录锁是固定 10 秒租约。`DistLock` 虽然提供 `renewLock`，登录流程没有续租。
4. `login_count +1`、写 `uip_`、写 `usession_` 是多条 Redis 命令，返回值没有逐项检查，
   不是事务或 Lua 原子更新。
5. 普通断线回调在获取锁失败、`usession_<uid>` 不存在或 ID 不匹配时会提前返回，不会在
   该分支继续调用 `CServer::ClearSession`。异常情况下可能残留本机 `_sessions` 或错误的
   在线数；未登录连接的 uid 为 0，也会经过这套清理逻辑。
6. 写失败路径先调用 `ClearSession`，Redis 路由依赖随后触发的读错误回调清理，职责分散。
7. 旧服返回成功只表示 1021 已进入发送队列，没有客户端确认机制。
8. 旧服找不到 session 时按幂等成功，但不会修正旧服可能已经失真的 `login_count`。
9. 踢人 RPC 失败缺少具体 gRPC 状态日志，好友申请 RPC 又使用了错误的踢人日志文案。
10. 当前没有自动服务发现、踢人重试和统一链路追踪；`uip_<uid>` 中的服务名必须能命中
    `[PeerServer]` 创建的连接池。

后续应优先**统一断线清理入口**，保证本机 session 一定注销；然后把 Redis 的
“比较 session ID 后删除或覆盖”收敛成 Lua 原子操作；最后补充路由 TTL、心跳和在线数校准。

