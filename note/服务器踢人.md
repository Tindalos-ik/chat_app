# 同服与跨服踢人

## 1. 目标与当前状态

同一个 uid 再次登录时，系统只保留新连接。旧端先收到 `ID_NOTIFY_OFF_LINE_REQ`，随后旧
socket 由服务端关闭；Qt 客户端收到通知后也会关闭连接并切回登录页。

当前代码已经接通同服和跨服两条链路：

| 场景 | 处理方式 |
| --- | --- |
| Redis 没有 `uip_<uid>` | 直接建立新会话，不发踢人 RPC |
| 旧会话在本服 | 从 `UserMgr` 找旧 session，通知下线并 `ClearSession` |
| 旧会话在另一台 ChatServer | 同步调用旧服 `NotifyKickUser`，成功后再登记新会话 |
| RPC 不可达、连接池缺失或超时 | 新登录返回 `RPCFaild`，不覆盖旧路由 |

> 主动关闭 socket 是互踢的最终强制手段。TCP 的 `TIME_WAIT` 是协议正常状态，不能把“不由
> 服务端主动关闭”当成消除 `TIME_WAIT` 的保证；这里更重要的是先尽力送达下线通知，再关闭。

## 2. 为什么需要分布式锁

两台 ChatServer 可能同时处理同一 uid 的登录、掉线和路由更新。当前登录临界区使用
`login_lock_<uid>`；`DistLock` 在 Redis 中实际保存为 `lock:login_lock_<uid>`，租约 10 秒，
最多等待 5 秒。

锁内顺序固定为：

```text
读取 uip_<uid>
  -> 踢掉旧会话（同服调用或跨服 RPC）
  -> login_count 新服 +1
  -> 写 uip_<uid>、usession_<uid>
  -> 写本机 UserMgr
  -> 释放锁
```

跨服 RPC 是同步的。新服在收到旧服成功响应前不会覆盖 Redis 路由，避免旧服清理时误删
刚写入的新位置。调用设置 3 秒 deadline，小于当前 10 秒锁租约，防止对端异常时无限占住
业务线程和登录锁。

## 3. RPC 契约

```protobuf
message KickUserReq {
    int32 uid = 1;
    string session_id = 2;
}

message KickUserRsp {
    int32 error = 1;
    int32 uid = 2;
}

service ChatService {
    rpc NotifyKickUser (KickUserReq) returns (KickUserRsp) {}
}
```

`session_id` 是新服在持锁期间从 `usession_<uid>` 读到的旧会话标识。旧服只有在本机当前
session 的 ID 与它完全一致时才执行踢人；字段缺失或不一致返回 `ServerBusy`，防止延迟 RPC
按 uid 误踢后来建立的新会话。旧服已经找不到该 uid 的 session 时按幂等成功返回，因为
“目标已不在线”已经满足踢人目的。服务对象通过 `RegisterServer` 持有实际 TCP `CServer`；
每个 ChatServer 进程只创建这一个 `CServer`，所以 gRPC 与 TCP 登录操作的是同一份会话表。

客户端封装默认把响应设为 `RPCFaild`，只有以下条件都满足才可能成功：

1. `uip_<uid>` 能命中 `_pools` 中的对端服务名；
2. 连接池能取到非空 stub；
3. RPC 在 3 秒 deadline 内返回 OK；
4. 对端当前 session_id 与请求携带的旧 session_id 相同；
5. 对端业务响应为 `Success`。

## 4. 旧会话如何真正失效

旧服处理顺序如下：

1. `CSession::NotifyOffline()` 把 `ID_NOTIFY_OFF_LINE_REQ` 放入该 session 的发送队列；
2. 设置 `_close_after_send`，之后普通业务 `Send` 不再入队；
3. `CServer::ClearSession` 从 `_sessions` 中注销旧会话，并让 `UserMgr` 仅在 session_id
   匹配时删除 uid 映射，然后让旧服 `login_count` 减一；
4. 发送队列排空后，`HandleWrite` 调用 `Close()` 关闭旧 socket；
5. 新服收到成功响应，覆盖 `uip_<uid>`、`usession_<uid>` 并让新服 `login_count` 加一。

客户端 `TcpMgr` 收到 1021 后发出 `sig_off_line`，`MainWindow::SlotOffline` 弹出提示、关闭
本地 TCP 连接并回到登录页。若 1021 与服务端关闭连接相邻、客户端没有完整解析出通知，
`QTcpSocket::disconnected` 也会在已登录状态下发出一次 `sig_connection_lost`，关闭连接并回到
登录页；两个事件通过 `_disconnect_notified` 去重。即使客户端不配合，服务端也会在通知写完
后关闭 socket。

## 5. 当前仍需继续防守的边界

当前正常互踢链路由分布式锁保证先清旧、后写新，但还不是生产级会话注册表：

- ChatServer 崩溃时可能留下 `uip_<uid>` 和不准确的 `login_count`；
- 锁只有固定租约，没有自动续租；若临界区超过 10 秒，互斥保证会减弱；
- 没有心跳、路由 TTL、服务发现与自动剔除，陈旧路由仍需人工或后续机制治理；
- 除踢人外，其他跨服 RPC 尚未统一 deadline、重试和链路追踪。

当前本地 `UserMgr` 删除和踢人 RPC 已校验 session_id；正常断线路径也会在持锁期间比较
`usession_<uid>` 后再删 Redis key。后续仍可用 Lua 把“比较 + 删除”收敛为单条 Redis 原子
操作，并给在线路由增加租约与心跳。
