# 私聊文件传输

## 设计

文件正文走 ResourceServer 的独立 TCP 连接，ChatServer 只接收资源 ID 和聊天元数据。这样文件不会挤进
ChatServer 的小型聊天 TCP 帧或跨服 gRPC；数据库和 SQLite 也只保存文件引用，不保存文件字节。

```text
Qt 客户端 --1003/1004 分片上传--> ResourceServer
    --1040 fileArray--> ChatServer --VerifyFiles gRPC--> ResourceServer
    <--1041 已落库确认-- ChatServer
接收方 <--1042 实时通知 / 1030 历史同步-- ChatServer
接收方点击文件卡片并选好保存路径
    --1007/1008 分片下载--> ResourceServer
```

单文件上限是 100 MiB。客户端在选择文件时检查一次，ResourceServer 也会在 1005 任务同步和每个
1003 分片请求中再次检查，超限返回 `1022 UploadFileTooLarge`。空文件当前不接受。

## 聊天消息

| 消息 ID | 方向 | 用途 |
| :--- | --- | --- |
| `ID_FILE_CHAT_MSG_REQ(1040)` | Qt -> ChatServer | 发送文件消息请求 |
| `ID_FILE_CHAT_MSG_RSP(1041)` | ChatServer -> 发送方 Qt | 资源核验并持久化后的确认 |
| `ID_NOTIFY_FILE_CHAT_MSG_REQ`(1042) | ChatServer -> 接收方 Qt | 文件消息实时通知 |

1040 信封使用 `fromuid`、`touid` 和 `fileArray`。数组项包含客户端生成的 `msgid` 与上传完成后得到的
`resource_id`。ChatServer 以当前登录 session 的 UID 验证发送者，不信任客户端提交的文件名、MIME 或大小。
它调用 `ResourceService.VerifyFiles` 核验资源任务已完成、元数据有效、发布文件存在且磁盘大小与任务元数据
一致，再将 ResourceServer 返回的名称、MIME、大小写入消息。

`1041` 成功表示消息已提交到 MySQL。接收方离线或实时推送失败时，发送方仍得到持久化成功确认，接收方之后
通过 `1029/1030` 增量历史同步取得文件卡片元数据。跨 ChatServer 的实时通知使用 `NotifyFileChatMsg`。
目标 ChatServer 需要部署包含此 RPC 与 1042 handler 的共享代码版本。

发送方收到成功的持久化确认后，也会将带服务端正式消息 ID 和会话 ID 的文件元数据写入本机 SQLite，
但不推进连续历史同步游标。这样后续收到已读通知时可以按消息 ID 更新文件气泡；若读游标先于持久化确认，
客户端会暂存本次会话的游标，并在确认入库时合并。历史同步返回已读状态的文件消息也会恢复为“已读”，
避免只恢复卡片却丢失回执显示。

`chat_message.message_type` 增加 `file`。文件复用 `resource_id`、`resource_name`、`mime_type` 和
`file_size` 字段，图片专用的 `width`、`height` 留空。新库执行 `sql/create_tables.sql`；已有库执行
幂等迁移 `sql/chat_image_message_migration.sql`。客户端 SQLite 将服务端核验后的紧凑元数据放入
`local_chat_message.content`，会话摘要显示为 `[文件]`。

## 手动下载与鉴权

实时通知和离线历史只显示文件名称、字节数和可点击卡片，不会自动下载。用户点击卡片并选择保存路径后，
客户端才使用 1007/1008 拉取 2 KiB 分片，并以 `QSaveFile` 原子写入该路径；成功后提示保存位置。传输失败会保留卡片，用户可以重新点击并再次选择保存路径。

每一个 1007 都携带当前 `uid`、`token`、`thread_id`、`resource_id` 和偏移量。ResourceServer 先通过
StatusServer 校验 token，再查询 MySQL 确认该用户属于此私聊，而且同一个 `thread_id` 下确有一条
`message_type='file'` 的消息引用该 `resource_id`。资源 ID 本身不是读取凭证。`1020` 表示身份、成员关系或
资源归属被拒绝；`1021` 表示鉴权依赖暂不可用，稍后可重试。

文件卡片实际显示后发送 `1036`，这只代表消息元数据已经进入聊天界面，不代表用户已经下载或打开文件。消息已读
仍由原来的 `1038/1039` 会话游标处理。
