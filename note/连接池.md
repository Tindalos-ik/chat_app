# 连接池模型总结

> 项目里一共有 4 类"池"，它们的核心思想一致——**预先创建一批资源，用"借-还"的方式复用**。

---

## 1. 概念：什么是连接池

每次"创建连接"都是有成本的：

- Redis：一次 TCP 连接 + AUTH 认证；
- MySQL：一次 TCP 连接 + 握手 + 认证 + 选库（比 Redis 更重，还有空闲超时断连问题）；
- gRPC：创建 Channel/Stub、建立 HTTP/2 连接；
- Boost.Asio：io_context + 线程的启动开销。

如果每个请求都新建、用完就销毁，会出现三个问题：

1. **慢**：高频请求下，大量时间花在建立连接上；
2. **贵**：连接数不可控，服务器资源（文件描述符、内存、数据库连接数上限）被耗尽；
3. **乱**：多线程各自持有连接，无法统一管理。

连接池的做法：**启动时一次创建 N 个连接放进池子，需要时"借"，用完"还"，池空则等待**。这就是经典的**生产者-消费者模型**（线程是消费者，池是资源仓库）。

---

## 2. 连接池的通用模型

项目里 4 个池虽然管理的资源不同，但结构完全一样，固定由 5 部分组成：

```cpp
class XxxPool {
    std::queue<资源> _connections;   // ① 资源队列（空闲连接）
    std::mutex _mutex;               // ② 互斥锁：保护队列（queue 本身非线程安全）
    std::condition_variable _cond;   // ③ 条件变量：池空时让调用方挂起等待
    std::atomic<bool> _b_stop;       // ④ 停止标志：关闭池时唤醒所有等待线程

    资源 GetConnection() {           // ⑤ 借：加锁 -> 等待非空 -> 队首出队
        std::unique_lock<std::mutex> lock(_mutex);
        _cond.wait(lock, [this]{ return _b_stop || !_connections.empty(); });
        if (_b_stop) return nullptr;
        auto con = std::move(_connections.front());
        _connections.pop();
        return con;
    }

    void ReturnConnection(资源 con) { // ⑤ 还：加锁 -> 入队 -> 唤醒一个等待者
        std::lock_guard<std::mutex> lock(_mutex);
        if (_b_stop) return;          // 池已关闭，连接就地释放
        _connections.push(std::move(con));
        _cond.notify_one();
    }
};
```

关键细节：

- **`unique_lock` vs `lock_guard`**：借连接时要用 `unique_lock`（可以临时解锁让出 CPU，等 `condition_variable` 唤醒）；归还时用 `lock_guard`（不需要解锁，`notify_one` 即可）。
- **`b_stop_` 判断**：`wait` 的返回条件包含"池已关闭"，否则池空时关闭会导致线程永远挂死。
- **关闭流程**：`_b_stop = true; cond.notify_all();` 唤醒所有等待者，让他们拿到 `nullptr` 后退出。

---

## 3. 项目中的连接池全景

| 池 | 所在文件 | 管理的资源 | 池大小 | 借/还方式 |
| --- | --- | --- | --- | --- |
| `AsioIOServicePool` | `include/AsioIOServicePool.h` | `io_context` + 工作线程 | CPU 核心数（20） | round-robin 轮流分配，不需要归还 |
| `StatusConPool` | GateServer / ChatServer 的 `StatusGrpcClient` | `StatusService::Stub` | GateServer 10 / ChatServer 10 | 借出 unique_ptr，用完归还 |
| `RPConPool` | GateServer 的 `VarifyGrpcClient` | `VarifyService::Stub` | 5 | 借出 unique_ptr，用完归还 |
| `RedisConPool` | `include/RedisMgr.h` | hiredis `redisContext*` | 5 | 借出裸指针，用完归还 |
| `MySqlPool` | `include/MysqlMgr.h` | `mysqlx::Session` | config.ini `poolsize`（5） | 借出 unique_ptr，RAII 自动归还 |

---

## 4. 逐个解析

### 4.1 AsioIOServicePool：IO 服务池（线程池）

严格说它不是"连接池"，而是 **io_context 工作线程池**，为每个连接分配一个独立的 IO 上下文：

```cpp
class AsioIOServicePool : public Singleton<AsioIOServicePool> {
    std::vector<boost::asio::io_context> _ioServices;  // N 个 io_context
    std::vector<executor_work_guard<...>> _workGuards; // 防止 run() 提前退出
    std::vector<std::thread> _threads;                 // N 个线程，各跑一个 ioc
    std::atomic<std::size_t> _nextIOService;           // round-robin 索引
};
```

作用：

- **负载均衡**：`GetIOService()` 用原子自增取模，把新连接轮流分到不同线程；
- **work_guard**：没有任务时 `run()` 会立即返回，work_guard 让它一直等待；
- **Stop()**：先 `reset()` 再 `stop()` 最后 `join()`，优雅退出。

它体现的是"**池化线程/上下文**"的思想——连接池池化的是连接，这里池化的是执行环境。

### 4.2 StatusConPool / RPConPool：gRPC Stub 连接池

两个类的结构一模一样，只是资源类型不同（`StatusService::Stub` / `VarifyService::Stub`）：

```cpp
StatusConPool::StatusConPool(size_t poolsize, std::string host, std::string port) {
    for (size_t i = 0; i < poolsize; ++i) {
        std::shared_ptr<Channel> channel =
            grpc::CreateChannel(host + ":" + port, grpc::InsecureChannelCredentials());
        _connections.push(StatusService::NewStub(channel));  // 池里预先放好 N 个 Stub
    }
}
```

调用方使用：

```cpp
auto stub = _pool->getConnection();          // 借
Status status = stub->GetChatServer(&context, request, &reply);
Defer defer([&stub, this]() {
    _pool->returnConnection(std::move(stub)); // 还（Defer 保证异常也不漏还）
});
```

作用：

- **复用 Channel**：gRPC Channel 是长连接，避免每次 RPC 都建连；
- **线程安全**：多个 IO 线程同时调 RPC，从池里取，互不干扰；
- **上限可控**：并发 RPC 最多等于池大小，多出的请求排队等待。

### 4.3 RedisConPool：hiredis 连接池

RedisMgr 里的池，管理 hiredis 的 C 指针（`redisContext*`），构造时 `redisConnect` + `AUTH` 成功后入池：

```cpp
class RedisConPool {
    std::queue<redisContext*> connections_;   // 存裸指针
    ...
};
```

每个 Redis 操作都是固定套路——**借连接 → 执行命令 → 释放 reply → 还连接**：

```cpp
bool RedisMgr::Set(const std::string &key, const std::string &value) {
    auto connect = _connectionPool->getConnection();   // 借
    if (connect == nullptr) return false;

    redisReply* reply = (redisReply*)redisCommand(connect, "SET %s %s", key.c_str(), value.c_str());
    if (reply == nullptr || 失败条件) {
        freeReplyObject(reply);                         // C 库要手动释放 reply
        _connectionPool->returnConnection(connect);     // 还
        return false;
    }
    freeReplyObject(reply);
    _connectionPool->returnConnection(connect);         // 还
    return true;
}
```

作用：

- 避免每个命令都新建 TCP 连接（hiredis 是 C 库，连接释放必须 `redisFree`）；
- 单例 + 池，解决多线程并发访问 Redis 的连接管理问题。

> 注意：hiredis 的 `redisContext` 是裸指针，所有失败分支都要记得 `returnConnection`，项目里每个方法都手工保证借还成对。

### 4.4 MySqlPool：MySQL 会话池（最完整）

管理 `mysqlx::Session`（X DevAPI），比其他池多了**连接有效性检测**和 **RAII 自动归还**：

```cpp
// 借：取出的连接先验证，无效则重建
std::unique_ptr<mysqlx::Session> MySqlPool::GetConnection() {
    std::unique_lock<std::mutex> lock(mutex_);
    cond_.wait(lock, [this]{ return !pool_.empty() || b_stop_; });
    if (b_stop_) return nullptr;

    auto con = std::move(pool_.front());
    pool_.pop();
    if (!con || !IsConnectionValid(con.get())) {   // SELECT 1 探测
        con = CreateSession();                     // 失效就新建替代
    }
    return con;
}

// 还：同样先探测，失效则重建再入池
void MySqlPool::ReturnConnection(std::unique_ptr<mysqlx::Session> con) {
    ...
    if (!con || !IsConnectionValid(con.get())) {
        con = CreateSession();
    }
    pool_.push(std::move(con));
    cond_.notify_one();
}
```

调用方用 `Defer` 保证归还：

```cpp
auto con = pool_->GetConnection();
Defer defer([&con, this]() { pool_->ReturnConnection(std::move(con)); });
```

作用：

- **解决 MySQL 空闲断连**：MySQL 连接长时间不用会被服务器断开，借还时用 `SELECT 1` 探测，失效自动重建；
- **unique_ptr 自动管理生命周期**：比裸指针安全，出作用域自动释放。

---

## 5. 各池对比小结

| 对比项 | AsioIOServicePool | gRPC Stub 池 | RedisConPool | MySqlPool |
| --- | --- | --- | --- | --- |
| 资源类型 | io_context+线程 | Stub | redisContext* | mysqlx::Session |
| 归还方式 | 不需要归还（轮询） | 显式归还/Defer | 显式归还 | Defer + unique_ptr |
| 有效性检测 | 无 | 无 | 无 | 有（SELECT 1 重建） |
| 内存管理 | RAII | unique_ptr 移动 | 裸指针手动释放 | unique_ptr |
| 池关闭 | join 线程 | notify_all 返回 nullptr | notify_all 返回 nullptr | notify_all 返回 nullptr |

---

## 6. 连接池在本项目中的作用总结

1. **性能**：省去高频场景下的建连/握手/认证开销（Redis AUTH、MySQL 握手、gRPC 建连、线程创建）；
2. **资源上限可控**：并发访问被限制在池大小内，不会把数据库/服务器的连接数打爆；
3. **线程安全**：单例 + 池，让多线程共享同一批资源而不冲突；
4. **优雅关闭**：`b_stop_ + notify_all` 让所有等待线程安全退出，配合单例析构释放资源；
5. **连接自愈**（MySqlPool）：检测到失效连接自动重建，提升稳定性；
6. **解耦与复用**：业务代码只关心"借-还"，不关心连接是怎么建、怎么关的。

---

## 7. 设计要点与踩坑

1. **借还必须成对**：漏还 = 池逐渐被借空，最终所有请求卡死在 `wait`。用 `Defer`/RAII 兜底最稳妥。
2. **`unique_lock` 才能配合条件变量**：等待时需要释放锁，`lock_guard` 做不到。
3. **关闭池要先 `notify_all`**：否则正在等待的线程永远醒不来。
4. **归还前检查 `b_stop_`**：池已关闭时连接直接释放，不要再塞回队列。
5. **移动语义**：`unique_ptr` 的 Stub/Session 入队出队必须 `std::move`，不能拷贝。
6. **C 资源（hiredis）要手工管理**：`redisReply` 用完 `freeReplyObject`，`redisContext` 用完 `redisFree`，注意每个失败分支。
7. **MySQL 连接会空闲断连**：只"借还"不够，还要 `SELECT 1` 探测 + 失效重建。
8. **池大小不是越大越好**：太大浪费资源（每个 MySQL/Redis 连接都有开销），太小并发排队；按业务压测调整（项目里多为 5~10，io_context 默认 CPU 核数）。
