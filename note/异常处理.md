# 异常处理

本文记录 ChatServer 面对网络错误、心跳超时和进程退出时的处理边界。异常处理不只是
`if (error)` 后关闭 socket，还必须保证异步回调、`io_context` 与会话对象的生命周期一致。

## 1. 断线入口

读错误、写错误、非法心跳包、心跳超时和 1021 下线通知发送完成，都会进入
`CSession::HandleDisconnect()`。

该入口通过 `_disconnect_handled.exchange(true)` 保证同一 Session 只执行一次完整清理：

```text
网络读写错误 ─┐
心跳超时     ─┼─> HandleDisconnect()
协议异常     ─┤      ├─ Close socket
发送完成     ─┘      ├─ 清理本机 Session / UserMgr / login_count
                         └─ 比较 session_id 后清理 Redis 路由
```

先清理本机状态，再处理 Redis。Redis 锁获取失败或 Redis 暂时不可用时，本机 `_sessions`
不能因此残留。

## 2. 为什么不能只在析构函数中 `clear()`

`steady_timer` 与 `async_accept` 都是异步操作。销毁 timer 或 acceptor 会取消未完成操作，
但取消回调仍可能由 `io_context` 以 `operation_aborted` 调度。

如果回调捕获裸 `this`：

```cpp
_timer.async_wait([this](const boost::system::error_code& ec) {
    on_timer(ec);
});
```

对象析构后，回调即使只准备检查 `operation_aborted`，调用成员函数时也已经使用了悬空
`this`。因此这不是空指针判断能解决的问题，而是 use-after-free。

同样地，清空 `_sessions` 不代表 `CSession` 一定析构：正在执行的读写回调和逻辑队列仍可能
持有 `shared_ptr<CSession>`。如果 Session 保存的是裸 `CServer*`，后续回调调用
`ClearSession()` 时也会访问已经释放的服务器对象。

## 3. 对象所有权

`CServer` 使用 `enable_shared_from_this`，由 `shared_ptr` 创建后再调用 `Start()`；构造函数
不注册异步操作，因为此时对象尚未被 `shared_ptr` 托管。

```text
main / LogicSystem / ChatServiceImpl  ── shared_ptr ──> CServer
CServer 的 timer / accept 回调       ── weak_ptr   ──> CServer
CServer 的 _sessions                 ── shared_ptr ──> CSession
CSession                              ── weak_ptr   ──> CServer
```

timer 和 accept 回调先 `weak_ptr::lock()`，成功后才调用 `CServer` 成员函数；服务器释放后，
取消回调只会结束，不会解引用失效对象。`CSession::HandleDisconnect()` 也先锁定服务器弱引用，
避免异步读写回调访问悬空的 `_server`。

### `weak_ptr` 在这里具体做什么

`weak_ptr` 是一种**观察引用**：它知道对象是否还活着，但不增加 `shared_ptr` 的引用计数，
因此不会决定对象的生死。

```cpp
std::weak_ptr<CServer> weak_server = weak_from_this();

_timer.async_wait([weak_server](const boost::system::error_code& ec) {
    if (auto server = weak_server.lock()) {
        // lock() 成功：临时得到 shared_ptr，整个回调期间 CServer 都不会析构。
        server->on_timer(ec);
    }
    // lock() 失败：所有 shared_ptr<CServer> 都已释放，不能再访问服务器成员，直接结束。
});
```

它在本项目中有两个位置：

1. **timer/accept 回调观察 CServer**：回调排队期间不延长服务器寿命。停止流程释放最后一个
   `shared_ptr<CServer>` 后，即使取消回调晚到，`lock()` 也会失败，不会访问悬空 `this`。
2. **CSession 观察所属 CServer**：`_sessions` 持有 Session，而 Session 只弱引用服务器，避免
   双方都用 `shared_ptr` 造成引用环；Session 的读写回调晚到时，也会通过 `lock()` 判断服务器
   是否仍可调用 `ClearSession()`。

> 回调若直接捕获 `shared_ptr<CServer>`，单次回调确实安全，但周期 timer 每次重新注册都会保留
> 一份强引用。外部已经要求停止时，最后一个 timer 回调仍能把 CServer 留住，形成“回调等对象
> 析构、对象析构又要等回调”的生命周期问题。因此周期任务使用 `weak_ptr`，仅在实际执行期间
> 用 `lock()` 短暂转成 `shared_ptr`。

## 4. 启动与停止顺序

`CServer` 的 `Start()` 在 `make_shared<CServer>` 后调用，开始 accept 和心跳扫描。退出时，
必须在栈上的 `io_context` 离开作用域前释放所有 `CServer` 的强引用：

```text
收到 SIGINT / SIGTERM
  -> CServer::Stop()：设置 stopping，取消 timer，取消并关闭 acceptor
  -> io_context.stop()，停止主循环
  -> AsioIOServicePool::Stop()，停止会话 I/O 线程
  -> gRPC Server::Shutdown()，等待 gRPC 线程退出
  -> LogicSystem::Stop()，业务队列排空并停止 worker
  -> ChatServiceImpl / LogicSystem 释放 shared_ptr<CServer>
  -> server.reset()，此时 CServer 析构，清空 _sessions
  -> io_context 离开作用域
```

`Stop()` 可重复调用。它不会在持有 `_mutex` 时调用 `HandleDisconnect()`，避免
`HandleDisconnect() -> ClearSession()` 再次获取 `_mutex` 形成自锁。

gRPC 启动失败也执行相同的解除引用步骤，避免错误路径让 `LogicSystem` 把 `CServer` 留到
`io_context` 之后才析构。

## 5. 心跳超时与客户端提示

客户端在最后一次有效 1024 后 60 秒仍未收到回复，会关闭本地 socket，并发出独立的
`sig_heartbeat_timeout`。主窗口只显示“心跳超时”提示，避免 `abort()` 触发的
`disconnected` 再显示一次普通“连接断开”。

直接关闭 ChatServer 时，操作系统会向客户端发送 FIN/RST，属于明确 TCP 断开，会立刻走
普通“连接断开”提示；暂停服务进程或静默丢弃回包才会走心跳超时。参数和复测场景见
[心跳检测.md](心跳检测.md)。

## 6. 当前边界

`CServer::Stop()` 负责停止 accept 和心跳扫描；进程退出时 Redis 的 `login_count` 会删除。
进程崩溃时无法执行这条路径，`uip_<uid>`、`usession_<uid>` 仍缺少 TTL/租约续期，可能保留
陈旧路由。这个问题需要 Redis 路由租约或原子条件删除进一步处理。
