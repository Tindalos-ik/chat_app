# 项目优化

## 将 ChatServer 逻辑层拆分为 LogicSystem 和 LogicWorker

### 改动目标

当前 ChatServer 的 `LogicSystem` 同时负责接收业务消息、维护队列、启动工作线程、注册消息回调，以及处理登录、好友、私聊和文本消息等全部业务。所有业务请求由单个线程串行执行：虽然 I/O 线程不会直接执行 MySQL、Redis 和 gRPC 调用，但一个慢 SQL 或跨服 RPC 仍会阻塞其他会话的业务处理。

将其拆分为 `LogicSystem` 和 `LogicWorker` 后，`LogicSystem` 只负责调度，`LogicWorker` 负责消费队列和执行具体业务。结构与 ResourceServer 保持一致：

```text
CSession
  -> LogicSystem：根据 session_id 选择固定 Worker
  -> LogicWorker：队列、工作线程、消息回调和业务处理函数
```

### 改动效果

1. 网络 I/O、任务调度和业务处理职责分离，`LogicSystem` 不再包含具体登录、好友和聊天业务。
2. 多个会话可由多个 Worker 并行处理，某个会话的慢 MySQL 查询或 gRPC 调用不会阻塞全部在线用户。
3. 同一个 `session_id` 固定分配给同一个 Worker，该会话上的登录、发送消息和后续请求仍按入队顺序串行执行，不会因多线程分发而乱序。
4. 每个 Worker 拥有独立队列、互斥锁和条件变量，队列竞争范围从全局缩小到单个 Worker。
5. ChatServer1 与 ChatServer2 使用相同的逻辑层模型，后续增加业务消息时只需在 `LogicWorker` 注册回调和实现处理函数。

### 改动方式

1. 新增 `LogicWorker.h/.cpp`。

   `LogicWorker` 持有一个业务队列、一个消费线程和消息 ID 到处理函数的回调表。将现有 `LogicSystem` 中的下列内容迁入 `LogicWorker`：

   - `DealMsg()`、`RegisterCallBacks()`；
   - 登录、搜索用户、好友申请、好友认证、创建私聊、文本消息、会话增量加载、消息历史加载和资料更新等处理函数；
   - `CServer` 指针及其设置接口。登录时跨服踢旧会话仍通过这一指针调用 `ClearSession()`。

2. 精简 `LogicSystem`。

   `LogicSystem` 保留 Worker 容器、`session_id -> worker 下标` 映射和轮询分配下标，并提供以下接口：

   - `BindSession(sessionId)`：新 TCP 连接建立后，以轮询方式为会话指定一个 Worker；
   - `UnbindSession(sessionId)`：连接关闭时删除映射；
   - `PostMsgToQue(node)`：按 `node` 中会话的 `session_id` 找到已绑定 Worker，再调用其 `PostTask()`；
   - `SetServer(server)`：将同一份 `CServer` 传递给所有 Worker；
   - `Stop()`：依次停止并等待所有 Worker 线程退出。

3. 修改 `CServer` 的会话生命周期。

   - `HandleAccept()` 在会话成功接入后，先调用 `LogicSystem::BindSession(sessionId)`，再将会话加入服务器映射并开始异步读；
   - `ClearSession()` 在移除会话后调用 `LogicSystem::UnbindSession(sessionId)`；
   - 这样同一会话从建立到断开的全部业务消息都由同一个 Worker 处理。

4. 修改启动和停止流程。

   主程序启动后调用 `LogicSystem::SetServer(server)`，使每个 Worker 都可执行需要服务器会话表的业务。退出时保持既有顺序：先停止 TCP 接收和 gRPC 服务，再停止 LogicSystem 的全部 Worker；Worker 退出后才释放 `CServer`。

5. 同步修改两台 ChatServer 的构建配置。

   在 `ChatServer1/CMakeLists.txt` 和 `ChatServer2/CMakeLists.txt` 中加入 `src/LogicWorker.cpp`。两台服务的业务代码和调度策略必须保持一致，避免同服与跨服场景的并发模型不一致。

### 并发约束

固定 Worker 只保证单个 TCP 会话内的处理顺序。不同会话会并发执行，因此好友认证、私聊创建、重复登录和跨服消息等涉及多个用户的数据操作，仍必须依赖已有的 MySQL 事务、唯一索引、Redis 分布式锁和线程安全的共享管理器保证一致性。

