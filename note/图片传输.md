# 图片传输

## 1. 为什么图片不能直接走聊天 TCP

ChatServer 的客户端 TCP 包体有严格长度上限，适合携带聊天业务 JSON，不适合承载图片
二进制。把图片 Base64 后直接塞进聊天消息不仅容易超过帧上限，还会让 ChatServer 的内存、
持久化和跨服 RPC 都承担不必要的大对象压力。

因此当前实现把图片传输拆成两条通道：

```text
图片字节：Qt 客户端 <---- ResourceServer TCP 9090 ----> 上传目录
图片元数据：Qt 客户端 <---- ChatServer TCP 8090/8091 ----> MySQL / ChatServer gRPC
资源核验：ChatServer <----------- ResourceServer gRPC 50057
```

聊天服务只保存并转发 `resource_id`、文件名、MIME、大小和尺寸；真正的图片字节始终只
经过 ResourceServer。

## 2. 整体链路

```text
发送方 Qt
  │ 选择、粘贴或拖入图片
  │
  ├─ ResourceServer TCP：分片上传，完成后取得 upload_id
  │
  ├─ ChatServer TCP 1033：发送 resource_id 和候选图片元数据
  │
  ├─ ChatServer --gRPC VerifyImages--> ResourceServer
  │       只接受已完成的 PNG/JPEG/GIF/WebP，并重新读取可信元数据
  │
  ├─ MySQL：写入 image 类型 chat_message
  │
  ├─ 发送方 TCP 1034：返回已持久化的 message_id/thread_id
  │
  └─ 接收方：同服 TCP 1035，或 ChatServer gRPC 后再 TCP 1035
          │
          └─ ResourceServer TCP 1007/1008：分片下载 -> 本地缓存 -> PictureBubble
```

接收方离线时，图片消息已经写入 `chat_message`。下次登录时，仍通过已有的
`ID_LOAD_CHAT_MSG_REQ/RSP`（1029/1030）按消息游标拉取图片元数据，然后再下载资源。

## 3. ResourceServer 协议

### 3.1 上传与资源标识

现有上传协议使用稳定的 `upload_id` 标识任务。图片聊天把这个值称为 `resource_id`，两者
是同一个不透明标识。业务消息不能保存 ResourceServer 主机的绝对文件路径。

上传完成后的 `resource_url` 为头像旧逻辑保留；聊天图片统一依赖 `resource_id`，因为接收
方可能不在 ResourceServer 所在机器。

### 3.2 下载分片

| ID | 方向 | 含义 |
| ---: | --- | --- |
| `1007` | Qt -> ResourceServer | 请求一个资源分片 |
| `1008` | ResourceServer -> Qt | 返回一个资源分片或错误 |

请求：

```json
{
  "resource_id": "upload_id",
  "offset": 0,
  "chunk_size": 2048
}
```

成功回包中的 `data` 是 Base64；`offset` 按原始字节计算。客户端只能在分片写入成功后，以
`offset + 解码后的 data.size()` 请求下一片。`is_last=true` 表示最后一片。

单片原始数据最多 2048 字节，为 ResourceServer 的 4 KB JSON 帧和 Base64 开销预留空间。
下载端使用 `QSaveFile` 写入临时文件，全部完成并通过图片解码验证后才原子提交到缓存目录。

服务端不会按客户端给出的路径读取文件：它先校验 `resource_id`，再从自己的任务目录、
`meta.json` 和最终文件解析资源。读取未完成、丢失或大小不一致的资源会失败。

## 4. ChatServer 与 ResourceServer 的 gRPC

`proto/message.proto` 中新增 `ResourceService.VerifyImages`。ChatServer 收到 1033 后，先把
每个 `resource_id` 交给 ResourceServer 批量核验；任意一个资源无效，整批图片均不会入库。

核验成功后返回的文件名、MIME、字节数、宽度与高度才是可持久化和可转发的数据。客户端请求
中同名字段只用于协议完整性检查，不能覆盖 ResourceServer 的结果。

ResourceServer 默认监听：

```ini
[ResourceServer]
rpcport = 50057
```

ChatServer1、ChatServer2 的各自 `config.ini` 都应配置对应的 `host` 和 `rpcport`。同步脚本不
复制实例配置，部署到多机时必须按实际网络地址分别修改。

## 5. 图片聊天消息协议

| ID | 方向 | 含义 |
| ---: | --- | --- |
| `1033` | Qt -> ChatServer | 图片消息请求 |
| `1034` | ChatServer -> 发送方 Qt | 图片消息已核验、已落库的确认 |
| `1035` | ChatServer -> 接收方 Qt | 图片消息实时通知 |

信封使用 `fromuid`、`touid` 和 `imageArray`。数组项包含：

```json
{
  "msgid": "客户端 UUID",
  "resource_id": "upload_id",
  "name": "photo.png",
  "mime_type": "image/png",
  "file_size": 12345,
  "width": 640,
  "height": 480
}
```

`1034` 与 `1035` 额外带有服务端确认后的 `message_id`、`thread_id`、`sender_id`、`recv_id`、
`created_at_ms` 和 `status`，与文本消息一致，供客户端 SQLite 去重、历史同步和失败气泡关联。

跨 ChatServer 时，源服务器调用 `NotifyImageChatMsg`。这层 gRPC 同样只传图片元数据，不传
图片字节；目标服务器再向本机客户端发出 1035。

## 6. 数据库存储

`chat_message` 新增如下字段：

| 字段 | 作用 |
| --- | --- |
| `client_msg_id` | 客户端 UUID，关联发送确认与本地状态 |
| `message_type` | `text`、`image`、`system` |
| `resource_id` | 已核验的 ResourceServer 上传任务 ID |
| `resource_name`、`mime_type` | 可信资源描述 |
| `file_size`、`width`、`height` | 已发布图片的实际属性 |

已有数据库执行一次：

```powershell
mysql -uroot -p chat_app_db < sql/chat_image_message_migration.sql
```

新部署统一执行 `sql/create_tables.sql`，不需要额外迁移。历史文本消息的
`message_type` 默认为 `text`，图片专用字段允许为空，因此不会破坏旧记录。

## 7. 客户端行为与失败语义

`ChatImageTransferTask` 串行管理图片上传和下载任务，避免共用 `ResourceClient` 时多个任务的
分片回包混在一起。ResourceClient 的上传进度与错误回包携带 `upload_id`，头像上传和聊天图片
上传可以据此区分归属。

| 阶段 | 失败处理 |
| --- | --- |
| 图片读取、格式或尺寸无效 | 本地拒绝，不发送 1033 |
| 上传或续传失败 | 对应图片气泡显示发送失败状态 |
| 资源 gRPC 核验失败 | ChatServer 不落库，1034 返回失败 |
| ChatServer 实时投递失败 | 消息已落库；接收方下次增量同步仍可得到元数据 |
| 下载、校验或缓存写入失败 | 不显示损坏图片；保留失败状态，可重新请求下载 |

图片缓存以资源 ID 派生的本地文件名保存，并通过最终图片解码再次确认像素尺寸，避免只凭文件
头或扩展名把损坏数据当作图片显示。
